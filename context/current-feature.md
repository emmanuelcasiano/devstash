# Current Feature

<!-- Feature Name -->

## Status

<!-- Not Started|In Progress|Completed -->

## Goals

<!-- Goals & requirements -->

## Notes

<!-- Any extra notes -->

## History

<!-- Keep this updated. Earliest to latest -->

- **2026-08-17** — Initial Next.js Setup: project scaffolded with Create Next App, Tailwind CSS configured.
- **2026-08-18** — Dashboard UI Phase 1: ShadCN UI initialized, `/dashboard` route added with main layout, dark mode by default, top bar (logo, search, New Collection, New Item), and sidebar/main placeholders.
- **2026-08-19** — Dashboard UI Phase 2 (Completed): collapsible sidebar with Types and Collections (favorites + recent) sections, item type links to `/items/[type]`, sidebar header ("Navigation" label + collapse toggle) and pinned user avatar footer spanning full width, drawer-based sidebar on mobile via Sheet, and a fixed-viewport-height dashboard shell (`h-screen` with internal `main` scroll) so the sidebar always fills the full screen height. Fixed a pre-existing broken `--font-sans` mapping so the app now renders with the intended Geist font. Lint and `npm run build` pass.
- **2026-08-19** — Dashboard UI Phase 3 (Completed): main dashboard content built out with 4 stats cards (items, collections, favorite items, favorite collections), a recent collections grid (name, favorite star, item count, description, type icons), a pinned items section, and a 10-item recent items list, each item/collection rendered as its own card. Added the shadcn `card` component and expanded `mock-data.ts` with more items and multi-collection assignments so collections show 2-3 distinct types. Lint and `npm run build` pass.
- **2026-08-22** — Prisma + Neon PostgreSQL Setup (Completed): Prisma 7 wired up with `@prisma/adapter-neon` driver adapter (v7 requires driver adapters and removed `directUrl` from the schema, so `prisma.config.ts` points the CLI at a direct/unpooled `DIRECT_URL` while the app runtime client in `src/lib/prisma.ts` uses the pooled `DATABASE_URL`). Full initial schema added (`User`, `Account`, `Session`, `VerificationToken`, `Item`, `ItemType`, `Collection`, `ItemCollection`, `Tag`) with indexes and cascade deletes per `context/features/database-spec.md`. Ran `prisma migrate dev --name init` and `prisma db seed` against the Neon dev branch (never `db push`); `prisma migrate status` confirms the schema is in sync. Lint and `npm run build` pass. Merged to `main`, branch deleted.
- **2026-08-22** — Seed Data (Completed): rewrote `prisma/seed.ts` per `context/features/seed-spec.md` — seeds a demo user (`demo@devstash.io`, bcryptjs-hashed password at 12 rounds), the 7 system item types (existing logic preserved), and 5 collections (React Patterns, AI Workflows, DevOps, Terminal Commands, Design Resources) totaling 18 items across snippets, prompts, commands, and links with real URLs. Added `bcryptjs` as a dependency. Seed logic checks for existing rows before creating, so re-running `prisma db seed` doesn't duplicate data — verified by running it twice against the Neon dev branch and confirming row counts stayed the same. Lint and `npm run build` pass.
- **2026-08-22** — Dashboard Collections (Real Data) (Completed): added `src/lib/db/collections.ts` with `getRecentCollections()`, which fetches a user's collections plus their items/item types via Prisma and computes item count, a border color from the most-used content type, and the distinct list of type icons per collection. `RecentCollections.tsx` is now an async server component calling this function directly instead of importing `mockCollections`, and `CollectionCard.tsx` consumes the new `CollectionWithStats` shape. `/dashboard` was marked `force-dynamic` since it now reads live DB data (Next was statically prerendering it at build time otherwise). Verified against the seeded demo data over SSR HTML — all 5 collections render with correct item counts and border colors matching their most-used type. Lint and `npm run build` pass. Since NextAuth isn't wired up yet, collections are scoped by the seeded demo user's email (same pattern as `prisma/seed.ts`) — will need to switch to the real session user once auth is implemented.
- **2026-08-24** — Dashboard Items (Real Data) (Completed): added `src/lib/db/items.ts` with `getPinnedItems()`, `getRecentItems()`, and `getItemStats()`, plus `getCollectionStats()` in `collections.ts`, all queried via Prisma per `context/features/dashboard-items-spec.md`. `PinnedItems.tsx`, `RecentItems.tsx`, and `StatsCards.tsx` are now async server components consuming these instead of `mock-data.ts`; `ItemRow.tsx` renders the real item type color/icon and tags relation, and `PinnedItems` still renders nothing when there are no pinned items. `formatShortDate` now accepts a `Date` directly. Also enriched `prisma/seed.ts` with tags, `isPinned`, and `isFavorite` on 7 seed items across different collections/types, updating those fields via `connectOrCreate`/`update` on the found-or-created item so the script stays idempotent and backfills previously-seeded rows. Verified against the seeded demo data over SSR HTML — 3 pinned items, 4 favorite items, and tag badges (react, hooks, ai, code-review, git, css, etc.) all render correctly. Lint and `npm run build` pass.
- **2026-08-24** — Stats & Sidebar (Real Data) (Completed): added `getItemTypesWithCounts()` to `src/lib/db/items.ts` per `context/features/stats-sidebar-spec.md`, fetching the 7 system item types with real per-user item counts. `Sidebar.tsx` now takes `itemTypes`/`collections` as props (fetched in `dashboard/layout.tsx`, now `force-dynamic`) instead of importing `mock-data.ts`; type links go to `/items/[typename]`, recent (non-favorite) collections show a colored circle keyed to their most-used item type instead of an item count, favorites keep the star, and a "View all collections" link to `/collections` was added under the list. `StatsCards.tsx` was already on real data from the prior feature. Along the way, discovered `prisma/seed.ts` never set `isFavorite: true` on any collection (unlike the old mock data), so the sidebar's Favorites section was empty once wired to the database — fixed by marking React Patterns and Terminal Commands as favorites in the seed and backfilling via `prisma db seed` (idempotent update, same pattern as the item `isPinned`/`isFavorite` backfill). Verified against the seeded demo data over SSR HTML — all 7 type links/counts, the colored circles, the Favorites section, and the "View all collections" link render correctly. Lint and `npm run build` pass.
- **2026-08-28** — Add Pro Badge to Sidebar (Completed): Files and Image item types are Pro-only, so the sidebar Types list now flags them. Added `PRO_ITEM_TYPES` (a `Set` of `"file"`/`"image"`) and `isProItemType(name)` to `src/lib/constants/item-types.ts`. In `Sidebar.tsx`, each Pro type renders a subtle shadcn `Badge` (`variant="secondary"`, shrunk to `h-4`, `text-[0.625rem]`, muted foreground, uppercase `PRO`) right after the type label in the expanded sidebar and mobile drawer; the collapsed desktop rail has no room for a badge so its icon tooltip gets a ` · PRO` suffix instead. The `badge` shadcn component was already present in the repo, so nothing new was added there. Lint and `npm run build` pass.
- **2026-08-29** — Codebase Scanner Quick Wins (Completed): applied the low-risk cleanups from the `codebase-scanner` audit. (1) Extracted the duplicated, uncached `getCurrentUserId()` helper (and its `DEMO_USER_EMAIL` constant) out of `src/lib/db/items.ts` and `src/lib/db/collections.ts` into a single `src/lib/db/current-user.ts` wrapped in `React.cache`, so a dashboard render resolves the demo user once instead of ~7 times. (2) Converted the `renderItemTypeIcon` render-helper in `src/components/shared/ItemTypeIcon.tsx` into an `<ItemTypeIcon>` component (uses `createElement` internally to satisfy the new `react-hooks/static-components` lint rule) and updated the `CollectionCard.tsx` / `ItemRow.tsx` call sites. (3) `src/lib/prisma.ts` and `prisma/seed.ts` now throw a clear error when `DATABASE_URL` is unset instead of passing `undefined` into `PrismaNeon`. (4) Extracted `toItemWithType`'s inline structural parameter type into a named `PrismaItemWithRelations` interface. (5) Narrowed `formatShortDate` to `(date: Date)` and dropped the dead `new Date()` wrap. (6) Added `@@index([collectionId])` to `ItemCollection` via migration `20260829044205_add_item_collection_collectionid_index` (additive `CREATE INDEX` only), applied to the Neon dev branch with `prisma migrate dev`; `prisma migrate status` confirms in sync — `prisma migrate deploy` still needs to run against production at deploy time. `src/lib/mock-data.ts` was deliberately left untouched. No user-facing behaviour change. Lint and `npm run build` pass.
- **2026-08-31** — Auth Setup: NextAuth v5 + GitHub Provider (Phase 1) (Completed): wired up NextAuth v5 (`next-auth@5.0.0-beta.32`) with `@auth/prisma-adapter@2.11.3` per `context/features/auth-phase-1-spec.md`, using the split-config pattern for edge compatibility. `src/auth.config.ts` holds the edge-safe config (GitHub provider + a `session` callback that copies `token.sub` → `session.user.id`, no adapter); `src/auth.ts` calls `NextAuth()` with `PrismaAdapter(prisma)` (reusing the `@/lib/prisma` singleton), `session: { strategy: "jwt" }`, and spreads `authConfig`, exporting `handlers`/`auth`/`signIn`/`signOut`. `src/app/api/auth/[...nextauth]/route.ts` re-exports `GET`/`POST` from the handlers. `src/proxy.ts` builds its own `NextAuth(authConfig)` instance (edge, adapter-free) and `export const proxy = auth(...)` redirects unauthenticated requests to NextAuth's default sign-in page (`/api/auth/signin`) with a `callbackUrl`; `matcher: ["/dashboard/:path*"]`. `src/types/next-auth.d.ts` augments `Session["user"]` with `id: string`. No custom `pages.signIn` — the default Auth.js sign-in page is used. Documented `AUTH_SECRET` / `AUTH_GITHUB_ID` / `AUTH_GITHUB_SECRET` in `.env.example` (the local `.env` already had all three set). Verified against the running dev server: `GET /dashboard` → 302 to `/api/auth/signin?callbackUrl=…`, the default sign-in page renders a "Sign in with GitHub" button, `/api/auth/providers` reports the GitHub OAuth provider, `GET /` stays 200 (unprotected), and `POST /api/auth/signin/github` → 302 to `github.com/login/oauth/authorize` with the right `client_id`/`redirect_uri`/PKCE challenge. The interactive GitHub login round-trip back to `/dashboard` still needs a manual click-through. Dashboard queries are still scoped to the seeded demo user; switching them to the real session user is Phase 2/3. Lint and `npm run build` pass. Merged to `main`, branch deleted.
- **2026-08-31** — Auth Credentials: Email/Password Provider (Phase 2) (Completed): added a NextAuth Credentials provider alongside GitHub per `context/features/auth-phase-2-spec.md`, keeping the split-config pattern. `src/auth.config.ts` gains an edge-safe Credentials placeholder — it declares the `email`/`password` field metadata (so the default Auth.js sign-in page renders those inputs) but its `authorize` is a hard `() => null` because `bcryptjs`/Prisma can't run in the Edge runtime that also loads this config via `src/proxy.ts`. `src/auth.ts` defines the real `credentialsProvider` (guards that email/password are strings, looks the user up by lowercased email, returns `null` when the user has no `password` hash e.g. a GitHub-only account, then `bcrypt.compare`, returning `{ id, email, name, image }` on success) and substitutes it into the config via `authConfig.providers.map(p => typeof p !== "function" && p.id === "credentials" ? credentialsProvider : p)` — GitHub is passed as a bare function reference so the `typeof` check skips it. New `src/app/api/auth/register/route.ts` (`POST /api/auth/register`) reads a JSON body and runs a validation ladder (bad JSON / missing field / bad email regex / password < 8 chars / passwords not equal → 400), normalizes email to trimmed-lowercase, rejects an already-registered email with 409, hashes with `bcrypt.hash(pw, 12)` (same cost as `prisma/seed.ts`), inserts via `prisma.user.create`, and returns 201 with `{ id, name, email }` only; DB work is wrapped in try/catch that logs and returns a generic 500. No migration needed — `users.password` (`password TEXT`) already exists from the `init` migration, verified against the migration SQL. Verified against the running dev server: register returns 201 / 409 (duplicate) / 400 (mismatch, short password, missing fields, bad JSON); credentials sign-in via `callback/credentials` → 302 `/dashboard` with an `authjs.session-token`, `/api/auth/session` returns the user with the correct `id`, wrong password → 302 `signin?error=CredentialsSignin`; GitHub OAuth still lists in `/api/auth/providers` and still 302s to `github.com/login/oauth/authorize` with PKCE; the default sign-in page renders both the GitHub button and the email/password form. Known minor gap (not fixed): the `findUnique`-then-`create` duplicate check isn't atomic, so a rare race falls through to the generic 500 instead of a clean 409 (the `@unique` constraint still prevents a duplicate row). Testing created a real `test@test.com` user in the Neon `development` DB, left in place. Lint and `npm run build` pass. Merged to `main`, branch deleted.
- **2026-08-31** — Auth UI: Sign In, Register & Sign Out (Phase 3) (Completed): replaced the default Auth.js pages with custom UI per `context/features/auth-phase-3-spec.md`. New `(auth)` route group whose **server-component** layout (`src/app/(auth)/layout.tsx`) centers the form, shows the DevStash logo, and redirects already-authenticated users to `/dashboard`; the pages `/(auth)/sign-in` and `/(auth)/register` are server components that render client-leaf forms (`src/components/auth/SignInForm.tsx`, `RegisterForm.tsx`). `SignInForm` has controlled email/password inputs, client validation, `signIn("credentials", { redirect: false })` with an inline "Invalid email or password." alert, a "Sign in with GitHub" button (`signIn("github", { redirectTo })`), reads `callbackUrl` / `error` / `registered` search params, and links to `/register`. `RegisterForm` validates name / email format / password ≥ 8 / passwords match client-side, `fetch`es `POST /api/auth/register`, surfaces the route's `error` string, and on success `router.push`es `/sign-in?registered=1` (where a success banner shows). `src/components/auth/GitHubIcon.tsx` is an inline SVG because lucide-react dropped brand icons. `src/auth.config.ts` now sets `pages.signIn: "/sign-in"` (so `/api/auth/signin` and OAuth errors land on the custom page); `src/proxy.ts` redirects protected routes to `/sign-in` with a **relative** `callbackUrl` (`pathname + search`). Sidebar footer: replaced the `mockUser` avatar with the real session user via a new reusable `src/components/shared/UserAvatar.tsx` (renders GitHub `image` when present, otherwise up-to-two-letter initials from the name via exported `getUserInitials`); the footer is now a `DropdownMenu` (opens upward) with a name/email header, a **Profile** item linking to `/profile`, and a destructive **Sign out** item calling `signOut({ redirectTo: "/sign-in" })`. `user` is fetched with `auth()` in `dashboard/layout.tsx` and threaded through `SidebarAside` / `SidebarMobile` → `Sidebar` (new exported `SidebarUser` type). Note: the generated shadcn `DropdownMenuLabel` maps to base-ui `Menu.GroupLabel` which throws outside a `Menu.Group`, so the header is a plain `<div>` instead. New minimal `src/app/profile/page.tsx` (server component: `auth()` guard → redirect to `/sign-in?callbackUrl=/profile`, then avatar + name + email + "Back to dashboard"). Added shadcn `dropdown-menu` and `label` components (base-ui based, `style: "base-nova"`). Also folded in: `src/lib/db/current-user.ts` `getCurrentUserId()` now returns `session.user.id` from `auth()` instead of the hardcoded `demo@devstash.io` lookup, so all dashboard/sidebar queries are scoped to the signed-in user (all callers already null-guard). Verified with Playwright against the dev server: `/sign-in` + `/register` render; register rejects a password mismatch client-side then succeeds → `/sign-in?registered=1` banner; wrong password → error alert, correct password → `/dashboard`; `callbackUrl` round-trip; signed-in user hitting `/sign-in` bounces to `/dashboard`; sidebar shows "AL" initials for a GitHub-less account; dropdown opens, Profile navigates, Sign out clears the session and redirects to `/sign-in`; GitHub button 302s to `github.com/login/oauth/authorize` with the right `client_id` + PKCE (interactive round-trip still needs a manual click-through); logged in as `demo@devstash.io` the seeded 18 items/5 collections show, logged in as a fresh account the dashboard is empty. During testing the dev server needed an HMR nudge to pick up the `current-user.ts` change (stale module, not a code bug). Test user `phase3-test@example.com` / `password123` created in the Neon `development` DB, left in place. Lint and `npm run build` pass. Merged to `main`, branch deleted.
- **2026-09-01** — Email Verification on Register (Completed): new registrations now must confirm their email before they can sign in with a password. Added the `resend@^6.25.0` package. `src/lib/email/resend.ts` is a lazily-instantiated Resend client (`getResend()` throws a clear error if `RESEND_API_KEY` is unset) plus `getEmailFrom()` reading a new `EMAIL_FROM` env var; `src/lib/email/verification-email.ts` sends the confirmation message (HTML + text, the user's name HTML-escaped) and throws on a Resend error. `src/lib/auth/verification-token.ts` reuses the existing NextAuth `verification_tokens` table (identifier = email) — `createEmailVerificationToken()` writes a 32-byte hex token with a 24h expiry, replacing any prior token for that address inside a `$transaction`; `consumeEmailVerificationToken()` is single-use (deletes the row on read, returns `null` when the token is unknown or expired). `src/lib/auth/verification.ts` wraps token-issue + send in `issueAndSendVerificationEmail()` and derives the absolute link base from a new `APP_URL` env var (falling back to the request origin). No migration was needed — `verification_tokens` and `User.emailVerified` already exist from the `init` migration. `POST /api/auth/register` now issues + sends the link after `prisma.user.create`; a send failure is caught, logged, and the route still returns 201 with `verificationEmailSent: false` so the account isn't lost. New `GET /api/auth/verify-email?token=` consumes the token, sets `User.emailVerified = now()`, and redirects to `/sign-in?verified=1`; missing/invalid/expired/used tokens and a missing user redirect to `/sign-in?error=VerificationInvalid`. New `POST /api/auth/resend-verification` re-issues + resends for accounts that have a password and are still unverified, and always responds `{ ok: true }` on a well-formed request (400 on bad JSON / bad email, 500 only if the send itself throws) so it can't be used to enumerate registered emails. `src/auth.ts` gains `class EmailNotVerifiedError extends CredentialsSignin { code = "EmailNotVerified" }`; `authorize` throws it after a successful `bcrypt.compare` when `emailVerified` is null. The `auth.config.ts` edge placeholder is untouched, and the GitHub OAuth path (which never calls `authorize`) is unaffected. `SignInForm.tsx` reads `verified=1` (success banner), updated `registered=1` copy ("Check your email…"), and `error=VerificationInvalid`; on a failed credentials sign-in with `result.code === "EmailNotVerified"` — or on the invalid-link path — it shows an inline "Resend verification email" button that POSTs the entered email to the resend route and confirms with "…a new link is on its way." Also added `scripts/prune-users.ts` (separate `chore:` commit): deletes every user except `demo@devstash.io` plus their cascaded content, sweeps orphaned verification tokens and tags, dry-run by default with `--yes` to apply, prints the target DB host, aborts if the keep-target is missing. Verified with Playwright + Neon dev-branch SQL: register → `/sign-in?registered=1` banner + `emailVerified: null` user + 24h token row; unverified sign-in → "Verify your email address before signing in." + resend button (confirming `EmailNotVerifiedError.code` round-trips through `signIn(..., { redirect: false })`); token rotation (old link → `VerificationInvalid`); verify link → `verified=1`, `emailVerified` set, token deleted; verified sign-in → `/dashboard`; bogus token → `VerificationInvalid` + resend UI. **Known limitation:** Resend is in sandbox mode with the `onboarding@resend.dev` from-address and only delivers to the account owner (`casianoemmanuel2217@gmail.com`); real delivery to other recipients needs a verified domain at resend.com/domains and `EMAIL_FROM` switched to it — the register route degrades gracefully but the resend endpoint returns 500 when the send genuinely fails. Pre-existing password accounts with `emailVerified: null` can no longer sign in until backfilled (`prisma/seed.ts` still doesn't set `emailVerified`; `demo@devstash.io` was already verified from an earlier seed run). All non-demo users (including test accounts from this session) were pruned from the Neon `development` DB via the new script. Lint and `npm run build` pass. Merged to `main`, branch deleted.
- **2026-09-02** — Toggle Email Verification (Completed): added `EMAIL_VERIFICATION_ENABLED` as a master switch for the register-time email-verification requirement, so the app is usable while Resend is still sandboxed (no verified domain — only the account owner's address can receive mail). New `isEmailVerificationEnabled()` in `src/lib/auth/verification.ts` is the single source of truth: it returns `true` unless the env var is exactly the string `"false"`, so an unset var (including in production) keeps verification on. Three server code paths read it: (1) `POST /api/auth/register` — when off, the user is created with `emailVerified: new Date()` and no token/email is issued; the JSON response now carries `verificationRequired: boolean` alongside `verificationEmailSent` (which now starts `false` and is only set `true` inside the `if (verificationRequired)` branch). (2) `src/auth.ts` `authorize` — the `EmailNotVerifiedError` throw is now guarded by `isEmailVerificationEnabled() && !user.emailVerified`, so with the flag off even pre-existing `emailVerified: null` accounts can sign in. (3) `POST /api/auth/resend-verification` — returns `{ ok: true }` immediately when off (after the existing bad-JSON/bad-email 400s), never calling Resend. The edge `auth.config.ts` placeholder and the GitHub OAuth path are untouched; `verify-email` route left as-is (harmless — no tokens are minted when off, and a stale link still resolves). Client UI: `RegisterForm` now parses the success body and redirects to `/sign-in?registered=1&verify=0` when `verificationRequired === false`, else the existing `/sign-in?registered=1`; `SignInForm` reads `verify` and shows "Account created. You can sign in now." when it's `"0"`, otherwise the "Check your email…" copy (option 1 from the spec — no `NEXT_PUBLIC_` mirror var). `.env.example` documents the new var; no DB migration, no new packages. Verified against the running dev server + Neon dev-branch SQL: flag **off** → register returns `201 {verificationRequired:false}`, DB shows `emailVerified` set + 0 tokens, register UI lands on the "you can sign in now" banner, immediate credentials sign-in → `/dashboard`, a pre-existing `emailVerified:null` account also signs in → session established, `resend-verification` → `200 {ok:true}` with no Resend call; flag **on** (regression) → register returns `verificationRequired:true` with `emailVerified:null` + 1 token, unverified credentials sign-in → `302 …?error=CredentialsSignin&code=EmailNotVerified`. Lint and `npm run build` pass. Merged to `main`, branch deleted. Test users from verification were deleted, then `scripts/prune-users.ts --yes` cleared all remaining non-demo users from the Neon `development` DB (4 users + 2 verification tokens removed; only `demo@devstash.io` remains). Local `.env` ended the session with `EMAIL_VERIFICATION_ENABLED='true'` (re-enabled by the user).
- **2026-09-02** — Forgot Password / Password Reset (Completed): self-service password reset for email/password accounts. New `src/lib/auth/password-reset-token.ts` reuses the NextAuth `verification_tokens` table with a `password-reset:<email>` identifier prefix — `createPasswordResetToken()` writes a 32-byte hex token with a **1-hour** TTL, replacing any prior reset token for that address in a `$transaction`; `consumePasswordResetToken()` is single-use (row deleted on read), returns `null` for an unknown/expired token or one whose identifier lacks the prefix, and strips the prefix before returning the email. The prefix keeps reset rows from colliding with the email-verification rows that `verification-token.ts` wipes via `deleteMany({ where: { identifier: email } })`. `src/lib/email/password-reset-email.ts` sends the link via Resend (HTML + text, name HTML-escaped, throws on Resend error); `src/lib/auth/password-reset.ts` `issueAndSendPasswordResetEmail()` wires issue + send and builds `${getAppBaseUrl(request)}/reset-password?token=…`, pointing at the **page** (not an API route — unlike `verify-email`). New `POST /api/auth/forgot-password` normalizes the email, 400s on bad JSON / bad email, and only sends when `user.password` is set (GitHub-only accounts are skipped). **Deviation from the spec:** a Resend send failure is caught, logged, and the route still returns `{ ok: true }` (pattern borrowed from the register route) instead of the 500 that `resend-verification` returns — with Resend sandboxed, a 500 for `demo@devstash.io` vs 200 for an unknown address would leak which emails are registered. New `POST /api/auth/reset-password` runs a validation ladder (bad JSON / missing token → 400 `code:"InvalidToken"` / missing fields / password < 8 / mismatch → 400), consumes the token (unknown/expired/used → 400 `InvalidToken`), fetches the user (missing → `InvalidToken`), `bcrypt.hash(pw, 12)`, updates `password`, and additionally sets `emailVerified: new Date()` **only when it was null** (reaching a link from the account's own inbox proves ownership); generic 500 on unexpected DB errors. New `(auth)/forgot-password` and `(auth)/reset-password` pages are server components (the latter wraps its form in `Suspense` for `useSearchParams`) rendering client leaves: `ForgotPasswordForm` (email input → neutral "if an account exists, a reset link is on its way" confirmation) and `ResetPasswordForm` (new/confirm password with the same ≥ 8 + match client validation as `RegisterForm`; an invalid-token state hides the form and links back to `/forgot-password`; success → `/sign-in?reset=1`). `SignInForm.tsx` gains a "Forgot password?" link beside the password label and a `?reset=1` "Password updated. You can sign in now." banner matching the `verified=1` / `registered=1` style. No DB migration, no new packages, no new env vars (reuses `RESEND_API_KEY` / `EMAIL_FROM` / `APP_URL`); `auth.config.ts` (edge) and the GitHub OAuth path are untouched; the new routes are outside the `proxy.ts` matcher. Verified against the running dev server + Neon dev-branch SQL: forgot-password → `{ ok: true }` for an unknown email, `demo@devstash.io`, and (after the swallow fix) a genuine send failure alike, with a `password-reset:`-prefixed 64-hex-char token row at ~60-min expiry; reset-password validation ladder returns the right errors incl. `InvalidToken` for missing/garbage tokens; a real token → `{ ok: true }`, password re-hashed at `$2b$12$`, token row deleted, reuse → `InvalidToken`, `emailVerified` left untouched when already set; credentials sign-in with the new password → 302 + session, old password → `error=CredentialsSignin`. Browser UI walkthrough was left to the user (manual). Test user `reset-test@example.com` (password now `brandnew456`, `emailVerified` set) left in the Neon `development` DB. Lint and `npm run build` pass. Merged to `main`, branch deleted.
- **2026-09-03** — Profile Page (Completed): built out `/profile` per `context/features/profile-spec.md`, replacing the minimal name/email/avatar placeholder from the Phase 3 auth work. New `src/lib/db/user.ts` `getProfileUser()` selects the session user (via the existing `getCurrentUserId()` cache) and returns `{ id, name, email, image, createdAt, hasPassword }` — `password` is reduced to the `hasPassword` boolean and the hash is never returned. `src/lib/utils.ts` gains `formatLongDate()` ("August 22, 2026"). `src/app/profile/page.tsx` is now a `force-dynamic` server component: `auth()` guard → `/sign-in?callbackUrl=/profile` (and a second redirect if `getProfileUser()` is null), then parallel-fetches `getProfileUser`, `getItemStats`, `getCollectionStats`, `getItemTypesWithCounts` (all reused as-is from `src/lib/db`) and renders three sections — user info (reuses `UserAvatar`; name/email/"Member since"), Usage (two stat cards styled like `StatsCards.tsx` for total items + total collections, plus an "Items by type" list over all 7 system types including zero counts, each with its colored Lucide icon via `getItemTypeIcon`), and Account. `src/components/profile/ProfileAccountActions.tsx` is the only client leaf: `ChangePassword` renders **only when `hasPassword`** and is a toggle-revealed inline form (current / new / confirm, same ≥ 8 + match client validation as `ResetPasswordForm`) posting to a new `POST /api/auth/change-password`; `DeleteAccount` opens an `AlertDialog` whose destructive button stays disabled until the user types their exact email, then posts to a new `POST /api/auth/delete-account` and calls `signOut({ redirectTo: "/sign-in" })`. New `src/components/ui/alert-dialog.tsx` — no dialog/alert-dialog shadcn component existed, so added a thin wrapper over `@base-ui/react/alert-dialog` (the package was already a dep; parts are `Root`/`Trigger`/`Portal`/`Backdrop`/`Popup`/`Title`/`Description`/`Close`), styled to match `sheet.tsx` (backdrop + `data-starting-style`/`data-ending-style` transitions) and `card.tsx` (`bg-card`, `ring-1 ring-foreground/10`, `rounded-xl`). `POST /api/auth/change-password`: `auth()` → 401 if no session; validation ladder (bad JSON / missing fields / new < 8 / mismatch → 400); loads the user by `session.user.id`, 400 `code:"NoPassword"` if GitHub-only, `bcrypt.compare` the current password → 400 `code:"InvalidPassword"`, else `bcrypt.hash(new, 12)` + update; generic 500 in try/catch. `POST /api/auth/delete-account`: `auth()` → 401; body `{ confirmation }` must equal `session.user.email` case-insensitively or 400 (server-side guard on top of the dialog); `prisma.user.delete({ where: { id } })` relies on the schema's existing `onDelete: Cascade` to remove items/collections/accounts/sessions; generic 500 in try/catch. No DB migration, no new packages, no new env vars. The new API routes sit outside the `proxy.ts` matcher (`/dashboard/:path*`) and guard themselves with `auth()`; `/profile` keeps its own `auth()` redirect. Verified with Playwright against the dev server as `demo@devstash.io`: signed-out `/profile` → `/sign-in?callbackUrl=/profile`; signed-in profile renders "Demo User" / "Member since August 22, 2026" / 18 items / 5 collections / by-type snippet 4, prompt 3, command 5, note 0, file 0, image 0, link 6 (= 18); change-password form toggles open and a wrong current password shows the inline "Current password is incorrect." alert (route returns 400). Left to the user for manual check (they took over testing): the change-password happy path + re-sign-in, the full delete-account flow (email-match gating → cascade → sign-out redirect), and a GitHub-only account (change-password section hidden, GitHub avatar image). During testing, noticed that a stale JWT for a pruned user makes `/profile` bounce to `/sign-in` and then to `/dashboard` (the `(auth)` layout redirects an authenticated session) — not a bug in this feature, just an artifact of the phantom session. Lint and `npm run build` pass. Merged to `main`, branch deleted.
- **2026-09-04** — Rate Limiting for Auth (Completed): sliding-window rate limiting on all seven auth endpoints per `context/features/rate-limiting-spec.md`, closing the "No rate limiting or brute-force protection" High finding from the session's auth security audit. Added `@upstash/ratelimit@^2.0.8` + `@upstash/redis@^1.38.3`. New `src/lib/rate-limit.ts` lazily creates one Upstash client (cached on `globalThis`, same pattern as `src/lib/prisma.ts`) and one `Ratelimit.slidingWindow` limiter per endpoint, each namespaced by a `prefix` (`rl:signin`, `rl:register`, …). Everything **fails open**: if `UPSTASH_REDIS_REST_URL` / `UPSTASH_REDIS_REST_TOKEN` are unset the client is `null` and every helper returns "allowed"; a Redis call that throws is caught, logged, and also allowed — locking users out because the limiter is down would be worse than the abuse. Exports: `getClientIp(request)` (`x-forwarded-for` first entry → `x-real-ip` → `"unknown"` shared bucket), `checkRateLimit` / `enforceRateLimit` (consume a token; the latter returns a ready 429 `NextResponse` or `null`), `peekRateLimit` / `consumeRateLimit` / `resetRateLimit` (via the lib's `getRemaining` / `limit` / `resetUsedTokens` — for the failed-attempts-only paths), and `rateLimitResponse(reset)` (429 JSON `{ error: "Too many attempts. Please try again in X minutes." }` + `Retry-After` header). Wiring: **sign-in** in `src/auth.ts` `authorize` — new `class RateLimitError extends CredentialsSignin { code = "RateLimited" }`; `authorize(credentials, request)` peeks both `signIn` (5/15m per `ip:email`) and `signInIp` (30/15m per `ip`) before any DB work and throws `RateLimitError` if either is exhausted, consumes both counters on any auth failure (no-password or bad password), and on success clears **only** the `ip:email` counter — the IP counter is left to decay so one valid account on a shared IP can't reset the whole allowance. **register** 5/1h per `ip` at the top of `POST`. **forgot-password** / **resend-verification** 3/1h per `ip:email`, placed after email-format validation so malformed emails still 400 without consuming. **reset-password** / **verify-email** 10/15m per `ip` at the top. **change-password** 5/15m per `session.user.id` — peek runs after the `auth()` 401 guard and the 400 validation ladder, a token is consumed only on a wrong current password (`InvalidPassword`), and the counter is reset after a successful update; the `NoPassword` (GitHub-only) and 404 branches don't count. `SignInForm.tsx` maps `result.code === "RateLimited"` to "Too many sign-in attempts. Please wait a few minutes and try again." and its resend-verification handler now surfaces the route's `data.error`; `RegisterForm` / `ForgotPasswordForm` / `ResetPasswordForm` / `ChangePassword` already render `data.error`, so their 429s show as-is (auth forms use inline alerts, no toast pattern). `.env.example` documents the two new vars (the local `.env` already had real Upstash credentials). The edge `auth.config.ts` placeholder and the GitHub OAuth path are untouched; the auth API routes stay outside the `proxy.ts` matcher (`/dashboard/:path*`), so limiters run in-handler, not in edge middleware. No DB migration. Verified live against the real Upstash instance on the dev server (curl + a CSRF-cookie loop for the credentials callback): register 5 pass → 429; forgot-password 3 pass → 429, a different email still works (per-email key); sign-in 5 wrong passwords → `code=credentials`, 6th → `code=RateLimited`, and a **correct** password still succeeds after prior failures and then restores a full 5-failure budget (failed-only + reset-on-success confirmed twice); change-password 5 wrong current-pw → 429, unauthenticated still 401 (RL doesn't bypass the auth guard); reset-password 10 → 429; resend-verification 3 → 429; verify-email 10 → 429. **Caveats:** `Retry-After` reflects Upstash's next epoch-aligned window boundary rather than the full window, so a 1-hour-window 429 displayed "3 minutes" (approximately honest — capacity starts returning at that boundary); a correct-password-but-unverified sign-in is not rate-limited (it throws `EmailNotVerifiedError` before any consume/reset — valid credentials, not an attack); each request to these endpoints now adds one Upstash REST round-trip (~50–150 ms locally). During testing the existing dev server (PID 15056, on :3000) was killed and restarted so the `src/auth.ts` change loaded; test artifacts were only Redis counters (expire in 15 m–1 h) for `unknown`/test IPs — no Neon rows, no emails sent. The session's `.claude/agents/auth-auditor.md` and `docs/audit-results/AUTH_SECURITY_REVIEW.md` were deliberately left untracked (not part of this feature). Lint and `npm run build` pass. Merged to `main`, branch deleted.
- **2026-09-04** — Items List View (Completed): built the `/items/[type]` dynamic route per `context/features/item-list-view-spec.md`, filling in the sidebar Type links that had 404'd since the Stats & Sidebar feature. `src/lib/db/items.ts` gains `getItemsByType(typeSlug)` — resolves the system `ItemType` by name (also accepts a trailing "s", so both the sidebar's singular `/items/snippet` and a plural `/items/snippets` resolve), returns `{ itemType, items }` newest-first scoped to the current user via the existing `getCurrentUserId()` cache, or `null` so the page can `notFound()` on an unknown slug. New `src/components/items/ItemCard.tsx` is a server component built from the shadcn `Card` with a left border in the item-type color, reusing the icon-tile / pin-star / tag-badge markup from `ItemRow`. New `src/app/(app)/items/[type]/page.tsx` is `force-dynamic`: self-guards with `auth()` → `/sign-in?callbackUrl=${encodeURIComponent(\`/items/${type}\`)}` (the slug is user-controlled so it's URL-encoded), `notFound()` for unknown types, a header with the type icon + item count, a `grid-cols-1 md:grid-cols-2` grid with `auto-rows-fr` so every card is the same height (`ItemCard` is `h-full` with its tag row pinned via `mt-auto` — tag-less cards get trailing whitespace, the accepted tradeoff), an empty state, and a `generateMetadata` tab title. The `{label}s` heading pluralizes with a bare "s", correct for all 7 immutable system types. **Shared chrome refactor (user-requested mid-feature):** introduced an `(app)` route group so `/dashboard`, `/items/[type]`, and `/profile` all render inside the same `TopBar` + `Sidebar` shell. `src/app/dashboard/layout.tsx` moved to `src/app/(app)/layout.tsx` (renamed `AppLayout`, typed `{ children: React.ReactNode }` like `(auth)/layout.tsx`); `dashboard/page.tsx`, `items/`, and `profile/page.tsx` moved under `(app)/`. Route-group parens don't change URLs, so `/dashboard`, `/profile`, `/items/[type]` are all unchanged (build output confirms). The now-redundant `min-h-screen` wrapper was dropped from the profile and items pages since `<main>` already scrolls. `proxy.ts` matcher is untouched (still `/dashboard/:path*`) — the moved pages keep their own in-page `auth()` guards. Verified in the browser (dev server, `demo@devstash.io`): `/items/snippet` shows 4 cards in a 2-col grid with blue left borders + pins/stars/tags; `/items/notes` (plural) resolves to the empty Notes state; `/items/bogus` → 404; the sidebar "Link" link navigates to `/items/link` (6 emerald-bordered cards). The sidebar/header now present on the items and profile pages was left to the user for a manual visual check. No DB migration, no new packages, no new env vars. Lint and `npm run build` pass. Merged to `main`, branch deleted.
- **2026-09-06** — Vitest Unit Testing Setup (Completed): added Vitest as the project's unit test runner, scoped to server-side logic and utilities (not React components). Added `vitest@^5.0.0` as a dev dependency plus `test` (`vitest run`) and `test:watch` (`vitest`) npm scripts. New `vitest.config.mts` (`.mts` so it's an ESM module regardless of `package.json` type) sets `test.environment: "node"` and wires the `@/*` path alias to `src/` via `resolve.alias` so tests import exactly like app code. Convention established in `context/coding-standards.md`: test files sit next to the code they cover and use the `.test.ts` extension — never `.test.tsx` — which keeps component tests out of scope by naming alone, without an explicit Vitest `exclude`. Three example test files prove the setup end to end against existing pure code: `src/lib/utils.test.ts` (`cn`, `formatShortDate`, `formatLongDate`), `src/lib/constants/item-types.test.ts` (`isProItemType`, icon/color lookups), and `src/lib/rate-limit.test.ts` (`getClientIp` header precedence and the fail-open helpers with Upstash env unset) — 3 files, 14 tests, all passing. Docs updated so the workflow reflects the new capability: `context/ai-interaction.md` step 4 now says to write/update Vitest tests for changed server actions and utilities and to run `npm test` alongside `npm run build`, and the "don't commit" line now also requires tests to pass; `CLAUDE.md` lists the two new commands and points at the Testing section; the `feature` skill's `SKILL.md` gained a `test` action row in the table and an `argument-hint` entry (the `actions/test.md` describing that step already existed). No component testing library (jsdom, Testing Library) was installed — deliberately out of scope. No `"use server"` action files exist yet, so "server actions and utilities" currently means `src/lib/**` (and `src/actions/**` once real actions land). No DB migration, no runtime code changed. Lint, `npm test`, and `npm run build` pass. Merged to `main`, branch `feature/vitest-setup` deleted.
- **2026-09-06** — Item List View — Three Columns on Large Screens (Completed): widened the `/items/[type]` card grid so it now steps 1 → 2 → 3 columns across the sm / md / lg breakpoints (it was capped at two). The only code change is in `src/app/(app)/items/[type]/page.tsx`: the grid class went from `grid auto-rows-fr grid-cols-1 gap-4 md:grid-cols-2` to `... md:grid-cols-2 lg:grid-cols-3`, and the page container widened from `max-w-5xl` to `max-w-6xl` so three columns aren't cramped. `auto-rows-fr` equal-height rows and `ItemCard` are untouched. No server actions, utilities, or `src/lib/**` were changed, so no new Vitest tests (per the Testing standard, React components aren't unit-tested and there was no pure function to cover). Verified in the browser (dev server, `demo@devstash.io`) at three widths — 480px → 1 column, 820px → 2 columns, 1440px → 3 columns — with `auto-rows-fr` still giving every card in a row equal height. No DB migration, no new packages, no new env vars. Lint, `npm test` (3 files / 14 tests), and `npm run build` pass. Merged to `main`, branch `feature/item-list-three-columns` deleted.
- **2026-09-06** — Shared Max-Width Container for App Pages (Completed): the `(app)` layout's `<main>` had no width cap, so each page decided its own — `src/app/(app)/items/[type]/page.tsx` wrapped its content in `mx-auto ... max-w-6xl ... p-6` while `src/app/(app)/dashboard/page.tsx` (predating the `(app)` route-group chrome refactor and the `max-w-6xl` pattern) had only `flex flex-col gap-8` and stretched edge-to-edge. Applied the "Consistent" fix: `src/app/(app)/layout.tsx` now keeps `<main>` as the full-width scroll container (`flex-1 overflow-y-auto`) and wraps `{children}` in `<div className="mx-auto w-full max-w-6xl p-4 md:p-6">` — so the vertical scrollbar stays at the viewport edge but every app page shares one centered, capped content column. Then stripped the per-page duplicates: `items/[type]/page.tsx` root wrapper `mx-auto flex w-full max-w-6xl flex-col gap-8 p-6` → `flex flex-col gap-8` (grid and `ItemCard` untouched — still 1→2→3 columns); `profile/page.tsx` keeps its intentionally narrower `mx-auto w-full max-w-2xl` (nests fine inside the layout's `max-w-6xl`) but dropped its `p-6` so padding isn't applied twice; `dashboard/page.tsx` unchanged (inherits the cap). Pure layout change — no server actions or `src/lib/**` utilities touched, so no new Vitest tests (React components aren't unit-tested per the Testing standard). Verified in the browser (dev server, `demo@devstash.io`) at 1600px wide: dashboard content is now capped and centered instead of full-bleed, the items grid keeps its 3-column layout at the same width as before, and the profile card sits centered within the wider container. The narrow-viewport pass was left to the user (they took over). No DB migration, no new packages, no new env vars. Lint, `npm test` (3 files / 14 tests), and `npm run build` pass. Merged to `main`, branch `feature/shared-max-width-container` deleted.
- **2026-09-06** — Item Drawer (Completed): right-side slide-in `Sheet` that is the item detail view — there is no separate item page. Built per `context/features/item-drawer-spec.md`. **Data:** `getItemById(id)` added to `src/lib/db/items.ts` returns a new `ItemDetail` (extends `ItemWithType` with `contentType`, `content`, `url`, `fileUrl`/`fileName`/`fileSize`, `language`, `updatedAt`, and `collections: {id,name}[]`), scoped to the current user via the existing `getCurrentUserId()` cache and using `findFirst({ where: { id, userId } })` so another user's item id resolves to `null` — indistinguishable from an unknown id. New `GET /api/items/[id]` (`src/app/api/items/[id]/route.ts`, first route under `src/app/api/items/`) guards with `auth()` → 401, calls `getItemById` → 404, else `{ item }`; params typed inline as `{ params: Promise<{ id: string }> }` (no dynamic API `[id]` route existed before). **State:** `src/components/items/item-drawer-provider.tsx` is a client context (`openItemId` + `openItem`/`closeItem`); `ItemDrawerProvider` wraps the page content in `src/app/(app)/layout.tsx` (inside `<main>`) and `<ItemDrawer />` is rendered as its sibling, so the drawer is available on `/dashboard` and `/items/[type]` alike while the pages stay server components. **Drawer:** `src/components/items/ItemDrawer.tsx` (client) — `Sheet`/`SheetContent side="right"` with `className="w-full gap-0 p-0 sm:max-w-md"`; fetches `/api/items/${openItemId}` in a `useEffect` with an `AbortController`, keyed on `[openItemId, reloadKey]`. State is **id-tagged** (`loadedItem` / `errorId` carry the id they belong to; the render derives `item = loadedItem?.id === openItemId ? loadedItem : null`) so switching items never flashes the previous item's content and no `setState` runs synchronously in the effect body (satisfies the `react-hooks/set-state-in-effect` lint rule — the first attempt reset state at the top of the effect and failed lint). Loading → `DrawerSkeleton` (pulse bars); error → message + "Try again" button that bumps `reloadKey`. Body sections render conditionally: type/language `Badge`s, action bar, Description, Content (`<pre className="overflow-x-auto rounded-lg bg-muted p-4 text-xs">`), Link (`<a target="_blank">`), File (name + `formatFileSize`), Tags (badges), Collections (badges linking to `/collections/[id]`, closing the drawer on click), and a Created/Updated `<dl>` via `formatLongDate(new Date(...))`. **Action bar:** Favorite (star, yellow when `isFavorite`), Pin, Copy, Edit, Delete (trash, right-aligned, destructive color) per `context/screenshots/dashboard-ui-drawer.png`. **Only Copy is wired** (`navigator.clipboard.writeText` of content → url → description, with a 1.5s "Copied" confirm); Favorite/Pin/Edit/Delete are presentational buttons with no handlers — mutations need server actions and are a later feature (the spec scopes this to "drawer details display only"). **Cards:** `src/components/dashboard/ItemRow.tsx` and `src/components/items/ItemCard.tsx` became `"use client"` leaves wrapped in a `<button type="button" onClick={() => openItem(item.id)}>` with `hover:bg-muted/50` + a focus ring; their parent server components (`PinnedItems`, `RecentItems`, the items page) are unchanged. **Utility:** `formatFileSize(bytes)` added to `src/lib/utils.ts` (whole bytes with no decimal, KB/MB/GB/TB with one decimal, `"0 B"` for zero/negative/non-finite) with 3 new tests in `src/lib/utils.test.ts` (now 3 files / 17 tests). `getItemById` and the API route got no unit tests — consistent with every other `src/lib/db/**` function and API route in the repo (Prisma + `auth()`, no mocking harness). No DB migration, no new packages, no new env vars. Lint, `npm test`, and `npm run build` pass; `/api/items/[id]` shows in the build route list. Browser verification (drawer open/close, skeleton, per-type sections, Copy, the new auth/ownership guards on the route) was taken over by the user. Merged to `main`, branch `feature/item-drawer` deleted.
- **2026-09-07** — Item Drawer — Edit Mode (Completed): the drawer's Edit (pencil) button now flips the same `Sheet` inline from view to an edit form; there is still no separate item page. Built per `context/features/item-drawer-edit-spec.md`. **First `"use server"` file in the repo** — `src/actions/items.ts` `updateItem(itemId, input)` returns the project's `{ success, data } | { success, error }` shape: `auth()` guard → `updateItemSchema.safeParse` (Zod errors joined into the `error` string) → ownership-scoped query → refreshed `ItemDetail`, generic message in `catch`. **Validation is pulled into a pure module** `src/lib/validation/item.ts` (no Prisma/auth imports) so Vitest can cover it: `updateItemSchema` (`title` trimmed non-empty; `description`/`content`/`language` blank→`null` via a shared `optionalText` transform; `url` `z.url()`-or-`null` via `optionalUrl`; `tags` trimmed, non-empty, de-duped) plus `parseTagsInput(raw)` for the comma field. `src/lib/db/items.ts` gains `updateItem(id, data)` — resolves the current user, ownership `findFirst` (foreign/unknown id → `null`, nothing written), `prisma.item.update` with `tags: { set: [], connectOrCreate }` to fully replace tags, then returns `getItemById(id)`. **Drawer** (`src/components/items/ItemDrawer.tsx`): edit state is **id-tagged** like the existing `loadedItem` (`editingId === openItemId`), so switching items or closing the drawer drops edit mode; `onOpenChange` also clears it. Edit is entered by `startEdit()` seeding controlled `form` state from the loaded item. The action bar swaps to **Save**/**Cancel** (Save shows a `Loader2` spinner, disabled while `form.title.trim() === ""` or saving); Cancel just clears `editingId`. Fields: Title + Description + Tags always; Content (snippet/prompt/command/note), Language (snippet/command), URL (link) gated on `item.itemType.name` via `CONTENT_TYPES`/`LANGUAGE_TYPES` sets. On success the drawer updates `loadedItem` from the action's returned `ItemDetail` via a `toPayload()` that re-stringifies the `Date` fields (server-action serialization hands back real `Date`s; drawer state holds ISO strings), exits edit mode, `toast.success("Item updated.")`, and `router.refresh()` so the underlying cards reflect the change; on failure it shows an inline destructive alert **and** `toast.error`. Collections + Created/Updated stay read-only in edit mode. The content `<Textarea>` is `font-mono text-xs md:text-xs leading-relaxed` — the `md:text-xs` override is deliberate because the base `Textarea` has `md:text-sm`, which otherwise made the editing font larger than the read view (user asked for it smaller). **New deps:** `zod` (was only transitive; now explicit per coding standards) and `sonner@^2` — added `src/components/ui/sonner.tsx` (`<Toaster theme="dark">`, app is dark-only) mounted once in `src/app/layout.tsx`, and `src/components/ui/textarea.tsx` (plain shadcn-style textarea; no `@base-ui` textarea part exists). **Tests:** `src/lib/validation/item.test.ts` — 9 cases (schema happy path, title required, blank→null normalization, URL valid/blank/malformed, tag trim+dedupe; `parseTagsInput` split/dedupe/blank). `npm test` now 4 files / 26 tests. `updateItem` (action + query) got no unit tests — same rationale as `getItemById`: Prisma + `auth()`, no mocking harness. No DB migration, no new env vars. `src/actions/**` was already anticipated by the Vitest setup's path notes. Lint, `npm test`, and `npm run build` pass; the build route list is unchanged (server actions don't add routes). Browser verification (enter/exit edit, per-type fields, save→toast→refresh, validation errors, ownership) was taken over by the user. Merged to `main`, branch `feature/item-drawer-edit` deleted.
- **2026-09-07** — Item Delete (Completed): the drawer's previously presentational Delete (trash) button now deletes the item behind a shadcn `AlertDialog` confirmation. `src/lib/db/items.ts` gains `deleteItem(id): Promise<boolean>` — resolves the current user, ownership `findFirst` (foreign/unknown id → `false`, nothing deleted), then `prisma.item.delete`; the schema's `ItemCollection` join rows cascade and shared `Tag` rows are left in place (same policy as edit mode). `src/actions/items.ts` gains a second `"use server"` export `deleteItem(itemId)` returning the existing `ActionResult<T>` shape (`auth()` guard → scoped query → `{ success: true, data: { id } }` or `"Item not found."` / generic `catch` message). **Drawer** (`src/components/items/ItemDrawer.tsx`): `startDelete()` opens a controlled `AlertDialog` ("Delete this item?" / "&ldquo;{title}&rdquo; will be permanently removed. This can't be undone.") with a destructive **Delete** and an **outline** **Cancel**; a `Loader2` spinner shows on Delete and both buttons are disabled while the action runs. Delete state is **id-tagged** like `editingId` — `deleteForId` is set to `item.id` and the dialog is `open={deleteForId === openItemId}`, so switching items drops the pending confirm; it's also cleared in the `Sheet` `onOpenChange`. On success: clear `deleteForId`, `closeItem()`, `setEditingId(null)`, `toast.success("Item deleted.")`, `router.refresh()` so the server-rendered dashboard/items-list counts drop the row. On failure: dialog stays open with an inline destructive alert (same markup as the edit-mode `formError`) **and** `toast.error`. The `{item && (…)}` drawer body was wrapped in a fragment so the `AlertDialog` sits as its sibling. No new pure logic, so no new Vitest tests — same rationale as `updateItem` / `getItemById` (Prisma + `auth()`, no mocking harness); `npm test` stays at 4 files / 26 tests. No DB migration, no new packages (`AlertDialog` + `sonner` already in the repo), no new env vars. Lint, `npm test`, and `npm run build` pass; the build route list is unchanged. Verified with Playwright on the dev server as `demo@devstash.io`: opened the Docker Documentation drawer → Delete → confirm dialog showed the item title → confirmed → drawer closed, Total Items 18→17, sidebar Link 6→5, DevOps collection 4→3 items, item gone from Recent Items. Re-ran `prisma db seed` (idempotent) to restore the deleted seed row — dashboard back to 18 items. Merged to `main`, branch `feature/item-delete` deleted.
- **2026-09-07** — Item Create (Completed): the top-bar "New Item" button now opens a modal dialog that creates an item, per `context/features/item-create-spec.md` (the spec had been untracked as "unbuilt" — this feature re-adds it alongside every other tracked feature spec). **Validation** — `src/lib/validation/item.ts`: extracted the previously-inlined tag normalizer into a shared `tagList` transform (now used by both schemas), then added `CREATE_ITEM_TYPES` (`snippet`, `prompt`, `command`, `note`, `link` — File/Image are Pro-only and excluded), the `CreateItemType` union, and `createItemSchema` (same `optionalText` / `optionalUrl` / `tagList` field helpers as `updateItemSchema`, plus a `.refine()` that requires `url` when `type === "link"`, error keyed to the `url` path). **Query** — `src/lib/db/items.ts` `createItem(data: CreateItemInput)`: resolves the current user (via the `getCurrentUserId()` cache) and the system `ItemType` by name (unknown slug or no session → `null`, nothing written), derives `contentType` (`URL` for links, `TEXT` otherwise), stores `null` for the fields that don't apply to the chosen type (content/language on a link, url on a text type), `connectOrCreate`s tags, and returns the full `ItemDetail` via `getItemById(created.id)` so the client needs no second fetch. **Server action** — `src/actions/items.ts` gains a third `"use server"` export `createItem(input: unknown)` following the established `ActionResult<T>` shape: `auth()` guard → `createItemSchema.safeParse` (Zod issues joined into `error`) → `createItemQuery` → `{ success, data }` or generic `catch` message. **UI** — new `src/components/ui/dialog.tsx`, a shadcn-style wrapper over `@base-ui/react/dialog` (no Dialog component existed; `Sheet` is a separate `side`-anchored variant), styled to match `alert-dialog.tsx` (`bg-card`, `ring-1 ring-foreground/10`, `rounded-xl`, `data-starting-style` / `data-ending-style` scale+fade) with a built-in close button like `sheet.tsx` and `max-h-[calc(100dvh-2rem)] overflow-y-auto` so a tall form scrolls. New `src/components/items/NewItemDialog.tsx` (client): a `DialogTrigger`-rendered `<Button size="sm">` ("New Item"), a 5-way segmented type selector (icon + label buttons, `aria-pressed`, primary-tinted when active), always-shown Title (required, `autoFocus`) / Description / Tags fields, and Content (snippet/prompt/command/note), Language (snippet/command), URL (link, required) shown conditionally via local `CONTENT_TYPES` / `LANGUAGE_TYPES` sets (mirrors `ItemDrawer`'s edit mode). Submit is disabled until the title is non-empty and — for links — the URL is non-empty; on submit it calls the action with per-type-nulled fields and `parseTagsInput(form.tags)`, shows a `Loader2` spinner, and on success closes the dialog, resets the form (type back to `snippet`), `toast.success("Item created.")`, and `router.refresh()` so the server-rendered dashboard / items-list counts update; on failure an inline destructive alert **and** `toast.error`. `src/components/layout/TopBar.tsx` drops its static New Item `<Button>` (and the now-unused `Plus` import) for `<NewItemDialog />`. **Tests** — `src/lib/validation/item.test.ts` gains 6 `createItemSchema` cases (minimal snippet, unknown type rejected, empty title rejected, link requires URL / accepts URL, non-link URL stays null, tag trim+dedupe); `npm test` now 4 files / 32 tests. `createItem` (action + query) got no unit tests — same rationale as `updateItem` / `getItemById` (Prisma + `auth()`, no mocking harness). No DB migration, no new packages (`@base-ui/react`, `sonner` already deps), no new env vars; the action adds no route. Verified with Playwright on the dev server as `demo@devstash.io`: created a **link** (URL field required, Content/Language hidden) → drawer showed the URL + de-duplicated `testing` / `e2e` tags, sidebar Link 6→7; created a **snippet** with content + language → drawer showed the `typescript` badge + Content section (contentType TEXT), sidebar Snippet 4→5; the type selector resets to Snippet on reopen. Both test items were deleted afterward via the drawer — seed data left intact. Lint, `npm test`, and `npm run build` pass. Merged to `main`, branch `feature/item-create` deleted.
- **2026-09-07** — Code Editor (Monaco) for Snippets & Commands (Completed): built per `context/features/code-editor-spec.md` (now tracked alongside the other feature specs). **New `src/components/items/CodeEditor.tsx`** (`"use client"`): Monaco via `dynamic(() => import("@monaco-editor/react"), { ssr: false })` with a pulse-skeleton `loading` fallback, so it stays a client leaf and Monaco's assets load from the `@monaco-editor/loader` default CDN (jsdelivr) — no Turbopack worker config. `beforeMount` registers a custom `devstash-dark` theme (`base: "vs-dark"`) tuned to the app's neutral palette — `#171717` body (= `--card` dark = Tailwind `neutral-900`), muted gutter/line-numbers, translucent-white scrollbar slider. Chrome: a header bar with macOS red/`#ff5f57` · yellow/`#febc2e` · green/`#28c840` dots on the left and, on the right, the language label (falls back to `shell` for commands) + a Copy button with a 1.5s "Copied" state. Grow-to-fit height via `editor.onDidContentSizeChange` clamped to `[120, maxHeight]` (`maxHeight` prop, default 400) — past the cap Monaco's themed scrollbar takes over. IDE-dense type: `fontSize: 11`, `lineHeight: 16`, mono font, `minimap` off, `wordWrap: "off"`, `scrollBeyondLastLine: false`, `renderLineHighlight`/`contextmenu` gated on `readOnly`, `overviewRulerLanes: 0`. Props: `value`, `language`, `typeName`, `readOnly`, `maxHeight`, `onValueChange`. **New pure helper `src/lib/code-language.ts`** (no Prisma/auth, unit-tested): `toMonacoLanguage(language, typeName)` maps the free-text `Item.language` onto a Monaco language id (aliases `ts`→`typescript`, `bash`→`shell`, `c++`→`cpp`, …; unknowns pass through lowercased/trimmed — Monaco renders unknown ids as plain text without throwing; empty → `shell` for commands, else `plaintext`), plus `isCodeItemType(name)` / `CODE_ITEM_TYPES` (`snippet`, `command`). **Wiring:** `ItemDrawer.tsx` uses `<CodeEditor>` for snippet/command content in **both** the read view (replacing the `<pre>`, `readOnly`) and edit mode (replacing the `<Textarea>`); prompts/notes keep the `<pre>` / `<Textarea>`. The drawer passes `maxHeight={1200}` (`DRAWER_CODE_MAX_HEIGHT`) so almost any snippet shows in full before the editor scrolls internally — the drawer body already scrolls. `NewItemDialog.tsx` likewise swaps in `<CodeEditor>` for snippet/command content (keeps the default 400 cap since it lives in a modal). **Also folded in (user-requested during the feature):** (1) each `/items/[type]` page now shows a type-specific solid **New {Type}** button in the header (`flex justify-between`), rendered only for the 5 creatable types (`CREATE_ITEM_TYPES` — File/Image excluded); it passes `defaultType={itemType.name}` so the dialog opens with that type preselected. (2) `NewItemDialog` gains `defaultType?: CreateItemType` (default `"snippet"`) and `triggerLabel?: string` (default `"New Item"`) props; `TopBar` still renders the generic `<NewItemDialog />`. The type is re-applied on open and on close/reset. (3) New `ITEM_TYPE_COLORS` map + `getItemTypeColor(name)` in `src/lib/constants/item-types.ts` — the 7 system-type brand hexes keyed by lowercase name, mirroring the seeded `ItemType.color` values so a client component with no DB access tints icons exactly like the sidebar. The dialog's 5-way type selector icons are now tinted with it, and `<DialogTitle>` leads with the selected type's colored icon + "New {type}" (was a static "New item"). **Tests:** new `src/lib/code-language.test.ts` (7 cases — alias mapping, unknown passthrough, command/plaintext fallback, `isCodeItemType` incl. null/undefined); `src/lib/constants/item-types.test.ts` +2 for `getItemTypeColor` (brand lookup, case-insensitive, gray fallback). `npm test` now 5 files / 41 tests. `CodeEditor` and the dialog/page wiring got no tests — React components are out of scope per the Testing standard. **Deps:** `@monaco-editor/react@^4.7.0` (dependency), `monaco-editor@^0.56.0` (devDependency — TypeScript types only; the runtime loads Monaco from the CDN). No DB migration, no new env vars; the build route list is unchanged. Lint, `npm test`, and `npm run build` pass. Browser verification was left to the user (they test the UI manually). Merged to `main`, branch `feature/code-editor` deleted.
- **2026-09-08** — Markdown Editor (Completed): built per `context/features/markdown-editor-spec.md`. **New `src/components/items/MarkdownEditor.tsx`** (`"use client"`): a tabbed Write/Preview editor for the two prose item types (note, prompt), mirroring `CodeEditor`'s chrome — `bg-[#1e1e1e]` container with `border border-border`, a `bg-[#2d2d2d]` header bar, and a header Copy button with the exact same classes + 1.5s "Copied" state as `CodeEditor`. Write is a controlled `<textarea>` that auto-resizes between `MIN_HEIGHT` 120 and `maxHeight` (prop, default 400) via a `resize()` callback wired to `onInput` and a `useEffect` keyed on `[activeTab, value, resize]` (so it re-fits when entering edit mode with prefilled content or switching items); past the cap it scrolls. Preview renders GitHub Flavored Markdown through `react-markdown` + `remark-gfm` inside a `maxHeight`-capped `overflow-y-auto` pane, with a "Nothing to preview." fallback when the value is blank. `readOnly` (prop) hides the Write tab and forces `activeTab = "preview"`; edit mode defaults to `useState<Tab>("write")`. react-markdown v10 renders no raw HTML and strips `javascript:` URLs by default, so no sanitizer was added. **New pure helper `src/lib/markdown-item.ts`** (no Prisma/auth, unit-tested): `MARKDOWN_ITEM_TYPES` (`Set` of `"note"`/`"prompt"`) + `isMarkdownItemType(name)` (case-insensitive, null-safe), mirroring `code-language.ts`'s `isCodeItemType`. **Wiring:** `ItemDrawer.tsx` and `NewItemDialog.tsx` both gain an `isMarkdownType` branch — snippet/command → `CodeEditor`, note/prompt → `MarkdownEditor`, anything else keeps the plain `<Textarea>` / `<pre>`. In the drawer that covers edit mode (content field) and the readonly view (`<MarkdownEditor value={item.content} readOnly />` replacing the `<pre>`); in the dialog it covers the content field. **CSS** — `src/app/globals.css` gains a `.markdown-preview` block kept **outside any `@layer`** so its rules beat Tailwind's preflight reset (which strips heading sizes and list markers): sized/weighted h1–h6 (h1/h2 get an underline rule), blue (`#60a5fa`) links with hover underline, disc/decimal lists with indentation, left-border blockquotes, `rgba(255,255,255,0.1)` inline-code chips, `#171717` fenced-code blocks, bordered tables with a header-row background, plus `hr`/`img`/task-list styling. **Also folded in (user-requested during the feature):** a shared `.editor-scroll` scrollbar (also in `globals.css`, outside `@layer`) tuned to the Monaco slider colours from `CodeEditor.tsx` (thumb `#ffffff1a` → `#ffffff33` hover → `#ffffff4d` active, 10px, rounded, transparent track, `scrollbar-width: thin` for Firefox) — applied to the MarkdownEditor write textarea + preview pane, the `ItemDrawer` scroll body (`.editor-scroll` added to the `flex-1 … overflow-y-auto p-6` div), and any overflowing `pre`/`table` inside a rendered preview. **Deps:** `react-markdown@^10.1.0` + `remark-gfm@^4.0.1` (dependencies). **Tests:** new `src/lib/markdown-item.test.ts` — 5 cases (true for note/prompt, false for code/other types, case-insensitivity, null/undefined, exact set membership). `npm test` now 6 files / 46 tests. The component and dialog/drawer wiring got no tests — React components are out of scope per the Testing standard. No DB migration, no new env vars; the build route list is unchanged. Lint, `npm test`, and `npm run build` pass. Browser verification (Write/Preview toggle, per-type rendering, Copy, drawer readonly view, scrollbar appearance) was left to the user. Merged to `main`, branch `feature/markdown-editor` deleted.
- **2026-09-10** — File & Image Upload with Cloudflare R2 (Completed): built per `context/features/file-image-spec.md`. **Storage** — new `src/lib/r2.ts` is a lazy `S3Client` for R2's S3-compatible API (cached on `globalThis` like `src/lib/prisma.ts`; `endpoint: https://<R2_ACCOUNT_ID>.r2.cloudflarestorage.com`, `region: "auto"`), with `isR2Configured()` (all five `R2_*` env vars present), `uploadToR2` / `deleteFromR2` / `getR2Object` (the last buffers via `transformToByteArray()` — files are ≤10 MB so no stream plumbing), and `buildPublicUrl(key)` / `keyFromPublicUrl(url)` which round-trip the object key through the stored `fileUrl` (per-segment `encodeURIComponent`, strip the `R2_PUBLIC_URL/` prefix to recover the key). Added `@aws-sdk/client-s3` (dependency). **Validation** — new pure, unit-tested `src/lib/validation/upload.ts`: `UPLOAD_KINDS` (`file`/`image`), per-kind `maxBytes` (5 MB image / 10 MB file), allowed extension + MIME sets, `validateUpload({kind,fileName,mimeType,size})` (blank / `application/octet-stream` / `text/plain` MIME is accepted when the extension matches, since browsers are inconsistent for `.md`/`.yaml`/`.toml`/`.ini`), `sanitizeFileName` (strips directories + unsafe chars, falls back to `"file"`), `extensionOf`, `acceptAttribute`. `src/lib/validation/item.ts` gains `file`/`image` in `CREATE_ITEM_TYPES`, `FILE_ITEM_TYPES` + `isFileItemType`, and `fileUrl`/`fileName`/`fileSize` on `createItemSchema` with a `.refine` requiring all three for file/image types (`fileSize` is `z.number().int().positive().nullish()`). **API routes** — `POST /api/upload` (`auth()` → 401, 503 when R2 unconfigured, parse multipart, server-side `validateUpload`, `PutObject` to `uploads/<userId>/<uuid>/<sanitized-name>`, returns `201 {fileUrl,fileName,fileSize}`); `GET /api/items/[id]/download` (ownership-scoped via `getItemById`, recovers the key, streams the bytes back with `Content-Type` + `Content-Disposition: attachment` + `Cache-Control: private, no-store` — a server proxy so the browser never hits the storage host, sidestepping CORS and forcing a download). **DB** — `src/lib/db/items.ts` `createItem` now sets `contentType: "FILE"` + `fileUrl`/`fileName`/`fileSize` for file/image types (null content/url/language), and `deleteItem` selects `fileUrl` in the ownership check then best-effort `deleteFromR2` after the row is gone (a storage error is logged, never fails the delete). **UI** — new `src/components/items/FileUpload.tsx` (client): drag-and-drop zone + hidden `<input>`, client-side `validateUpload` before upload, `XMLHttpRequest` POST to `/api/upload` with `xhr.upload.onprogress` driving a real progress bar + cancel (`xhr.abort()`), and an uploaded state showing an `<img>` thumbnail (images) or a file-icon card (files) with size + Remove. `NewItemDialog.tsx` — `TYPE_OPTIONS` extended to all 7 types (selector grid `grid-cols-5` → `grid-cols-4`), renders `<FileUpload>` for file/image instead of the content/language/url fields, and gates submit on `upload !== null`; `resetForm` clears the upload. `ItemDrawer.tsx` — the old file line is replaced by an Image/File `Section`: images get an inline `max-h-80` preview, files get a name/size card, both get a **Download** link to `/api/items/<id>/download` (a plain `<a>` styled with `buttonVariants({variant:"outline",size:"sm"})` — a base-ui `<Button render={<a>}>` warns because it defaults `nativeButton` true); `copyValue` gained `item.fileUrl` in its fallback chain. **Tests** — new `src/lib/validation/upload.test.ts` (24 cases across `validateUpload` happy/size/extension/MIME/kind paths, `sanitizeFileName`, `extensionOf`, `isUploadKind`, `acceptAttribute`); `item.test.ts` updated (its "unknown type" case used `type:"file"`, now valid — switched to `"video"`) + 2 new file/image refine cases. `npm test` now 7 files / 64 tests. The R2 helpers, upload route, and download route got no unit tests — consistent with every other `src/lib/db/**` / API route in the repo (AWS SDK + Prisma + `auth()`, no mocking harness). `.env.example` documents the five `R2_*` vars (`R2_ACCOUNT_ID`, `R2_ACCESS_KEY_ID`, `R2_SECRET_ACCESS_KEY`, `R2_BUCKET_NAME`, `R2_PUBLIC_URL` — the local `.env` already had real values); no DB migration (`Item.fileUrl`/`fileName`/`fileSize` + `contentType` FILE exist from `init`). **Turbopack gotcha:** `next dev` never registered the new `src/app/api/items/[id]/download/` folder — not across a server restart, not after wiping `.next` — until an unrelated change inside the `[id]/` directory forced a re-scan, after which it served correctly; `next build` had it listed the whole time. During the feature the dev server (then PID 17716, later 24740) was killed and restarted so the new `@aws-sdk/client-s3` dependency resolved. Verified against the running dev server: `POST /api/upload` → 201 (real R2 write during a browser test), `GET /api/items/<id>/download` → 401 without a session and proper JSON errors (not the HTML 404) once registered; full browser walkthrough (drag-drop upload with progress, create file/image items, drawer image preview + Download) taken over by the user. Lint, `npm test`, and `npm run build` pass; the build route list adds `/api/upload` and `/api/items/[id]/download`. Merged to `main`, branch `feature/file-image-upload` deleted.
- **2026-09-10** — Image Gallery View (Completed): built per `context/features/image-display-spec.md`. The `/items/image` list now renders a thumbnail gallery instead of the generic `ItemCard` grid. **New `src/components/items/ImageCard.tsx`** (`"use client"`, mirrors `ItemCard`'s button-wrapper pattern): the whole card is a `<button>` calling `useItemDrawer().openItem(item.id)` so a click still opens the existing item drawer. Inside a `Card` (with `p-0` to kill the default `py-(--card-spacing)`, `overflow-hidden` so the zoom crops cleanly) sits an `aspect-video` box holding a plain `<img src={item.fileUrl}>` (the established pattern — `FileUpload.tsx` / `ItemDrawer.tsx` both use `<img>` with an `eslint-disable @next/next/no-img-element`; there is no `next/image` remote-patterns config) at `object-cover` with `transition-transform duration-300 ease-out group-hover:scale-105` for the subtle 5% hover zoom. Falls back to a centered `ImageIcon` tile when `fileUrl` is null. A truncated title caption sits below the thumbnail — a small addition beyond the terse spec so the gallery stays navigable (flagged to the user, who was fine with it). **`src/lib/db/items.ts`** — added `fileUrl: string | null` to the `ItemWithType` interface, `PrismaItemWithRelations`, and the `toItemWithType` mapper so list cards can show the picture; removed the now-redundant explicit `fileUrl` from `ItemDetail` (it inherits it from `ItemWithType`). No query change — `fileUrl` is a scalar already returned by the existing `include: { itemType, tags }` finds in `getPinnedItems` / `getRecentItems` / `getItemsByType`, and `getItemById` already selected it. **`src/app/(app)/items/[type]/page.tsx`** — a new `isImageGallery = itemType.name === "image"` branch renders `<ImageCard>` in a `grid grid-cols-2 gap-4 sm:grid-cols-3` (3 columns per the spec, 2 on the smallest screens) instead of the `ItemCard` `auto-rows-fr` grid; all other item types are untouched. No new pure logic (the change to `items.ts` is a passthrough field on a Prisma-backed function with no mocking harness), so no new Vitest tests — `npm test` stays at 7 files / 64 tests. No DB migration, no new packages, no new env vars; the build route list is unchanged. Lint, `npm test`, and `npm run build` pass. The seed data has zero image items and image creation needs real R2 uploads, so the empty state was verified with Playwright (`/items/image` → "No image items yet.") and the full gallery walkthrough (thumbnails, 3-col grid, hover zoom, drawer open) was taken over by the user. Merged to `main`, branch `feature/image-gallery-view` deleted.
- **2026-09-10** — File List View (Completed): built per `context/features/file-display-spec.md`. The `/items/file` list now renders a single-column, Google Drive / Dropbox style list instead of the generic `ItemCard` grid. **New `src/components/items/FileRow.tsx`** (`"use client"`): one row per file. A local `FILE_ICONS` map keys the existing pure `extensionOf()` (from `src/lib/validation/upload.ts`) to a Lucide glyph — `FileJson` for `.json`, `FileCode` for `.yaml`/`.yml`/`.toml`/`.ini`/`.xml`, `FileSpreadsheet` for `.csv`, `FileText` for `.pdf`/`.md`/`.txt`, plain `File` as the fallback — rendered via `createElement` to satisfy the `react-hooks/static-components` lint rule (same trick as `ItemTypeIcon.tsx`). The row shows the icon, the file name (`item.fileName`, falling back to `item.title`), the size (`formatFileSize`), and the upload date (`formatShortDate`, matching `ItemCard`), plus a trailing download button. Because an `<a>` can't nest inside a `<button>`, the click target is an `absolute inset-0` overlay `<button>` calling `useItemDrawer().openItem(item.id)`, and the download `<a>` (href `/api/items/<id>/download`, the pre-existing streaming endpoint) is a sibling with `relative z-10` + `onClick` `stopPropagation` so it sits above the overlay and never also opens the drawer; the text spans are `pointer-events-none` so clicks fall through to the overlay. `hover:bg-muted/50` on the row container gives the hover highlight. Responsive: the right-aligned size and date columns are `hidden sm:block`; below `sm` they collapse into a single `"59 B · Sep 10"` line under the file name. **`src/lib/db/items.ts`** — added `fileName: string | null` and `fileSize: number | null` to the `ItemWithType` interface, `PrismaItemWithRelations`, and the `toItemWithType` mapper so list rows can render them; both are scalar columns already returned by the existing `include: { itemType, tags }` finds in `getPinnedItems` / `getRecentItems` / `getItemsByType`, so no query changed. Removed the now-redundant explicit `fileName` / `fileSize` from `ItemDetail` (it inherits them from `ItemWithType`) — same cleanup the Image Gallery feature did for `fileUrl`. **`src/app/(app)/items/[type]/page.tsx`** — a new `isFileList = itemType.name === "file"` branch renders `<FileRow>` inside `<ul className="divide-y divide-border">` wrapped in an `overflow-hidden rounded-xl border` container, placed before the `isImageGallery` branch; all other item types keep the `ItemCard` grid. No new pure logic (the `items.ts` change is a passthrough field on a Prisma-backed function with no mocking harness; the icon map is component-local view logic and components are out of scope per the Testing standard), so no new Vitest tests — `npm test` stays at 7 files / 64 tests. No DB migration, no new packages, no new env vars; the build route list is unchanged. Verified with Playwright on the dev server as `demo@devstash.io`: seed data has zero file items, so created a `.md` (59 B) and a `.json` (28 B) through the New File dialog (real R2 uploads) → the list rendered both rows with a divider, extension-specific icons, right-aligned size/date, and working download links; clicking a row opened the `ItemDrawer`; at 390px wide the size/date collapsed to a single line under the name. Both test items were then deleted via the drawer (R2 objects cleaned up by `deleteItem`) — Neon `development` back to 0 file items. Lint, `npm test`, and `npm run build` pass. Merged to `main`, branch `feature/file-list-view` deleted.
- **2026-09-10** — Quick-Copy Button on Type-Page Cards (Completed): each `ItemCard` on `/items/[type]` (snippets, prompts, commands, notes, links) now shows a small copy button in its bottom-right corner, revealed on card hover or when the button takes keyboard focus (`opacity-0` → `group-hover:opacity-100` / `focus-visible:opacity-100`). Clicking it copies `content ?? url ?? description` — the same precedence as the item drawer's Copy button — via `navigator.clipboard.writeText`, swaps the icon to a check for 1.5s, and does not open the drawer (`event.stopPropagation()`); it isn't rendered when there's nothing to copy. To avoid nesting a `<button>` inside the card's existing open-`<button>`, `ItemCard`'s markup was wrapped in a new `relative` `<div>` and the copy button is a sibling positioned `absolute right-2 bottom-2`. `src/lib/db/items.ts` gained `content: string | null` and `url: string | null` on the `ItemWithType` interface (plus `PrismaItemWithRelations` and the `toItemWithType` mapper) so the card has the value to copy without a follow-up fetch; both are scalar columns already returned by the existing `include: { itemType, tags }` finds, so no query changed. The now-redundant explicit `content` / `url` were dropped from `ItemDetail` (it inherits them from `ItemWithType`). Scope was limited (per the user) to the type-page grid — `ItemRow` (dashboard), `FileRow`, and `ImageCard` are untouched. No new pure logic, so no new Vitest tests (`npm test` stays at 7 files / 64 tests); browser verification was taken over by the user. No DB migration, no new packages, no new env vars; the build route list is unchanged. Lint, `npm test`, and `npm run build` pass. Merged to `main`, branch `feature/card-quick-copy` deleted.
- **2026-09-10** — Audit Quick Wins (Completed): applied the six low-risk findings from a `codebase-scanner` audit; the larger findings (sidebar collection query over-fetch, `getRecentCollections` running twice per dashboard render, unbounded `/items/[type]` list, the download-proxy `fileUrl` trust boundary, JWT sessions never re-validated against the DB, `verify-email` mutating on GET, the 821-line `ItemDrawer.tsx`) were left out of scope as needing design/UI/infra work. (1) **URL scheme hardening** — `optionalUrl` in `src/lib/validation/item.ts` replaced its bare `z.url()` check with a new `isHttpUrl()` helper (`new URL()` parse + `protocol` must be `http:`/`https:`), so both `url` and `fileUrl` now reject `javascript:` / `data:` / `ftp:` / `vbscript:` values that were previously storable and rendered into `<a href>` / `<img src>` in the item drawer; the Zod message changed to "Enter a valid http(s) URL." (2) **`updateItem` field normalization** — the query in `src/lib/db/items.ts` now also selects `itemType.name` in its ownership check and forces `content`/`url`/`language` to `null` for the types they don't apply to (link keeps only `url`; file/image keep none of the three), mirroring `createItem`. The drawer's edit form already sends `null` for hidden fields, so this only hardens the path against a hand-crafted server-action call; `updateItemSchema` itself was left as-is because the edit payload carries no `type` to refine against. (3) **`resend-verification` response** — `src/app/api/auth/resend-verification/route.ts` now wraps the Resend send in an inner try/catch that logs and lets the handler return `{ ok: true }` (200), instead of the outer catch turning a send failure into a 500. A 500 there only ever fired for a registered-but-unverified address, making it a mild email-enumeration oracle; this matches the pattern `forgot-password` already uses. (4) **`(app)` layout auth guard** — `src/app/(app)/layout.tsx` now `redirect("/sign-in")` when there is no session, so `/dashboard` protection no longer rests solely on the `proxy.ts` matcher (`/profile` and `/items/[type]` already self-guard). Plain `/sign-in` with no `callbackUrl` since a layout can't cheaply read the current pathname; the proxy still supplies a proper `callbackUrl` for `/dashboard/*`. (5) **Shared auth constants** — new `src/lib/validation/auth.ts` exports `EMAIL_PATTERN`, `MIN_PASSWORD_LENGTH`, and `PASSWORD_LENGTH_MESSAGE`; the five auth API routes (`register`, `change-password`, `reset-password`, `forgot-password`, `resend-verification`) plus `SignInForm.tsx`, `RegisterForm.tsx`, `ResetPasswordForm.tsx`, `ForgotPasswordForm.tsx`, and `ProfileAccountActions.tsx` import from it instead of each re-declaring the regex / number / message. Constant-sharing only — the routes still hand-roll their JSON+field validation (the larger "convert every auth route to Zod" refactor was explicitly out of scope). (6) **`capitalize()` extracted** — moved the one-line title-case helper into `src/lib/utils.ts`; `ItemDrawer.tsx`, `src/app/(app)/items/[type]/page.tsx` (was a local `toTitleCase`), and `Sidebar.tsx` (was inlined) now import it. **Tests:** `src/lib/validation/item.test.ts` gained two cases — `updateItemSchema` accepts `http`/`https` and rejects the four non-http schemes, and `createItemSchema` rejects a file/image whose `fileUrl` uses a non-http scheme; `npm test` now 7 files / 66 tests. Goal 2 (Prisma query) and goals 3–6 got no unit tests, consistent with the rest of the repo. Also reverted an unrelated editor-made whitespace re-indent in `context/coding-standards.md` so it stayed out of the feature diff. No DB migration, no new packages, no new env vars; the build route list is unchanged. Lint, `npm test`, and `npm run build` pass. Merged to `main`, branch `feature/audit-quick-wins` deleted.
- **2026-09-10** — Refactor: Break Up Large Components & Dedupe Shared UI Primitives (Completed): pure internal refactor from a "check for large blocks we can split" pass — no user-facing behavior change, no route/dependency/DB changes. **`ItemDrawer.tsx` 816 → 280 lines**, split into `src/components/items/use-item-detail.ts` (the `useItemDetail(openItemId)` fetch hook — `AbortController` + id-tagged `loadedItem`/`errorId`/`reloadKey`, plus the exported `ItemDetailPayload` type and `toPayload()`), `ItemActionBar.tsx` (read-view Favorite/Pin/Copy/Edit/Delete row, owns its own `copied` state, parent passes `key={item.id}`), `ItemEditFields.tsx` (presentational edit fields; shows content/language/URL based on `isContentItemType`/`isLanguageItemType`), `ItemDetailView.tsx` (read-mode sections + `ItemFileSection` for the image-preview/file-card/download block + `ItemMetaSections` for Collections + Created/Updated, which render in both modes), `DeleteItemDialog.tsx` (self-contained: owns `deleting`/`error`, calls the `deleteItem` action + toast, blocks close while pending), `DrawerSection.tsx` (the `<DrawerSection label icon>` helper), `ItemContentField.tsx` (the CodeEditor/MarkdownEditor/Textarea-or-`<pre>` switch, previously ~30 duplicated lines in the drawer and the new-item dialog), and `item-form.ts` (`ItemFormValues` type, `EMPTY_ITEM_FORM`, `DRAWER_CODE_MAX_HEIGHT = 1200`). The shell keeps the id-tagged `editingId`/`deleteForId` toggles and the edit-form state and renders the `Sheet` + header + one of `ItemEditFields`/`ItemDetailView` + `ItemMetaSections` + `DeleteItemDialog`. **Shared primitives:** `src/components/ui/form-message.tsx` (`<FormError>` red `role="alert"` box + `<FormNotice>` muted confirmation box — replaced 8 inline destructive alerts + 4 notice banners across the auth forms, item drawer, new-item dialog, and profile page), `src/components/ui/field.tsx` (`<Field label htmlFor>`, was defined identically in the drawer and dialog), `src/lib/http.ts` (`postJson<T>(url, body)` → `{ ok, status, data, error }`, swallows network throws + non-JSON body; folded the `fetch` + `(await res.json().catch(() => null)) as …` dance in 6 auth-form call sites; `error` holds only the server message so callers keep their own fallback copy), `src/lib/forms.ts` (`makeFieldUpdater(setForm)` → `updateField(name)` → an `onChange` handler; replaced 3 hand-written closures incl. `RegisterForm`'s), and `src/components/shared/StatTile.tsx` (the tinted-icon-tile stat card shared by `StatsCards.tsx` and the profile Usage section, which had it inline twice). **`SignInForm.tsx` 270 → 203**: extracted `src/components/auth/ResendVerification.tsx` (owns `sent`/`pending`/`error`, POSTs via `postJson`; parent bumps a `key` per sign-in attempt so it re-mounts fresh — replaces the old `setResendDone(false)` reset). `RegisterForm`/`ForgotPasswordForm`/`ResetPasswordForm`/`ProfileAccountActions` now use `postJson` + `FormError`/`FormNotice` (the delete-account dialog keeps its plain-text error — different style). **`Sidebar.tsx`**: main component body ~270 → ~110 lines via `SidebarSection` (the `Collapsible` + chevron chrome), `SidebarTypeLink`, `SidebarCollectionLink`, and `SidebarCollectionGroup` (one component for both the Favorites and Recent lists, which differ only by a trailing star vs. colored dot) — removes the collapsed/expanded `Tooltip`-vs-`Link` fork triplication; type/collection icons now render through the existing `<ItemTypeIcon>` to satisfy the `react-hooks/static-components` rule (a bare `const Icon = getItemTypeIcon(...)` inside a named sub-component trips it, unlike inside an inline `.map`). Total Sidebar file grew ~85 lines from the helper boilerplate — accepted tradeoff for removing the dup. **`src/lib/db/items.ts`**: `getItemById` now returns `{ ...toItemWithType(item), contentType, language, updatedAt, collections }` instead of re-mapping all 12 base fields; `createItem`/`updateItem` share a new `contentFieldsForType(typeName, data)` helper for the per-type `content`/`url`/`language` nulling (link keeps only `url`; file/image keep none; text keeps content+language, never url). That helper was **moved into `src/lib/validation/item.ts`** (pure, Prisma-free) so it can be unit tested — anything importing `src/lib/db/**` pulls in `src/lib/prisma.ts`, which throws at import when `DATABASE_URL` is unset (Vitest doesn't load `.env`), which is why nothing under `src/lib/db/**` has tests. **`src/actions/items.ts`**: `requireUserId(verb)` (`auth()` guard → `{ userId } | { error }`) and `zodMessage(error)` (flatten Zod issues) + a `GENERIC_ERROR` const replace the guard/message/try-catch shape repeated in all three actions. **`src/lib/validation/item.ts`** also gained `CONTENT_ITEM_TYPES`/`isContentItemType` and `LANGUAGE_ITEM_TYPES`/`isLanguageItemType` (mirrors the existing `isFileItemType` pattern) so the drawer and dialog stop hand-rolling `new Set([...])`. **Tests:** new `src/lib/forms.test.ts` (`makeFieldUpdater`, 2 cases) + 7 cases in `src/lib/validation/item.test.ts` for the two new predicates and `contentFieldsForType`; `postJson` (wraps `fetch`, no mocking harness in the repo) and `zodMessage` (one-line `.join`) left uncovered; extracted React components are out of scope per the Testing standard. `npm test` 7 files / 66 tests → 8 files / 75 tests. Behavior-preservation was verified by diffing the extracted markup/control flow against the originals; browser click-through of the drawer (view/edit/delete), auth forms, and sidebar was left to the user. Lint, `npm test`, and `npm run build` pass; build route list unchanged. Merged to `main`, branch `feature/refactor-large-components` deleted.
- **2026-09-10** — Collection Create (Completed): the previously inert "New Collection" top-bar button now opens a modal dialog that creates a user-scoped collection, following the same patterns as item create (no `context/features/` spec — goals generated from the inline request). **Validation** — new pure, Prisma-free `src/lib/validation/collection.ts`: `createCollectionSchema` (`name` trimmed + `min(1, "Name is required.")`, `description` via the same blank-to-`null` `optionalText` transform used in `validation/item.ts`) plus the inferred `CreateCollectionInput` type. **Query** — `src/lib/db/collections.ts` gains `createCollection(data): Promise<CollectionWithStats | null>`: resolves the current user via the existing `getCurrentUserId()` cache (→ `null`, nothing written, when there's no session), `prisma.collection.create` with `name`/`description`/`userId`, and returns the `CollectionWithStats` shape the dashboard lists consume — a brand-new collection has no items so `itemCount: 0`, `types: []`, and `color` falls back to a new `DEFAULT_COLLECTION_COLOR = "#6b7280"` const (also swapped into `getRecentCollections`'s old inline `?? "#6b7280"`). **Server action** — new `src/actions/collections.ts` `createCollection(input: unknown)` on the project's `ActionResult<T>` contract: `requireUserId` guard → `createCollectionSchema.safeParse` (Zod issues joined via `zodMessage`) → `createCollectionQuery` → `{ success, data }` or generic `catch` message. **Shared-helper extraction** — `ActionResult`, `requireUserId`, `zodMessage`, and `GENERIC_ERROR` were private to `src/actions/items.ts`; moved them into a new **plain (non-`"use server"`)** module `src/actions/shared.ts` (a `"use server"` file may only export async functions, so the type + const can't live there) and both action files now import from it. `items.ts` keeps `export type { ActionResult } from "@/actions/shared"` so the existing `@/actions/items` imports in `NewItemDialog.tsx` / `ItemDrawer.tsx` / `DeleteItemDialog.tsx` still resolve. **UI** — new `src/components/collections/NewCollectionDialog.tsx` (`"use client"`, first file under `src/components/collections/`): a `DialogTrigger`-rendered `<Button variant="outline" size="sm">` with the `FolderPlus` icon (same look as the button it replaces), a shadcn `Dialog` (reusing `Field`, `FormError`, `makeFieldUpdater`, `Textarea`) with Name (required, `autoFocus`) + Description (3-row textarea); submit is disabled until the name is non-empty, shows a `Loader2` spinner while the action runs, and on success closes the dialog, resets the form, `toast.success("Collection created.")`, and `router.refresh()` so the server-rendered dashboard picks it up; on failure an inline `FormError` **and** `toast.error`. `src/components/layout/TopBar.tsx` drops its static New Collection `<Button>` (and the now-unused `FolderPlus` import) for `<NewCollectionDialog />`. **Tests** — new `src/lib/validation/collection.test.ts` (5 cases: minimal payload + name trim, empty/whitespace name rejected, missing name rejected, blank description → `null`, description trimmed); `npm test` 8 files / 75 tests → 9 files / 80 tests. The action + query got no unit tests, consistent with the rest of `src/actions` / `src/lib/db` (Prisma + `auth()`, no mocking harness). No DB migration (`Collection` model unchanged), no new packages, no new env vars; the build route list is unchanged (server actions add no routes). Verified with Playwright on the dev server as `demo@devstash.io`: opened the modal (Create disabled until Name filled) → submitted "Playwright Test Collection" with a description → dialog closed, the new collection appeared in the **Recent Collections** grid ("0 items" + description, sorted first as newest) and the sidebar Collections → Recent list, and the **Collections** stat card went 5 → 6. Lint, `npm test`, and `npm run build` pass. Merged to `main`, branch `feature/collection-create` deleted. **Test data left in place:** "Playwright Test Collection" remains in the Neon `development` DB — there is no delete-collection UI yet.
- **2026-09-11** — Add Item to Collection(s) (Completed): a multi-select collection picker on the new-item dialog and the item drawer's edit mode, wiring the existing `ItemCollection` join table into the create/update flows (inline request, no `context/features/` spec). Explicitly out of scope: `/collections/[id]` or any other collection-browsing page. **Validation** — `src/lib/validation/item.ts` gains a `collectionIdList` transform (defaults to `[]`, de-duplicates) applied as `collectionIds` on both `createItemSchema` and `updateItemSchema`; an empty array is valid, since an item doesn't have to belong to any collection. **Query** — `src/lib/db/items.ts` gains `resolveOwnedCollectionIds(userId, collectionIds)`, which filters a submitted id list down to collections the current user actually owns (skipped entirely for an empty list) so a hand-crafted payload can never link an item into another user's collection; `createItem` uses it to nested-`create` `ItemCollection` rows on the new item, and `updateItem` uses it to fully replace memberships via `deleteMany: {}` + `create` (mirroring how `tags` is already replaced). **Options source** — `src/lib/db/collections.ts` gains a new, dedicated `getCollectionOptions()` returning lightweight `{id, name}[]` sorted alphabetically — deliberately not derived from the heavier, capped `getRecentCollections()` the sidebar uses, so the picker always reflects every collection the user has. **UI** — new `src/components/items/CollectionPicker.tsx`: a `DropdownMenu` + `DropdownMenuCheckboxItem` multi-select (menu stays open across selections via the primitive's `closeOnClick: false` default) showing "Select collections" / the comma-joined selected names / "No collections yet" (disabled) in its trigger. Wired into `NewItemDialog.tsx` (new `collectionIds` state, reset alongside the rest of the form, included in the `createItem` action payload) and into `ItemEditFields.tsx` / `ItemDrawer.tsx` (new `collectionIds` state seeded from `item.collections` in `startEdit()`, included in the `updateItem` action payload). `collections` is threaded down as a prop from the two server components that render these forms: `src/app/(app)/layout.tsx` (top-bar dialog + drawer, via the new `getCollectionOptions()` call alongside the existing `getRecentCollections()`/`getItemTypesWithCounts()` fetches) and `src/app/(app)/items/[type]/page.tsx` (its own type-scoped "New {Type}" button, fetched in parallel with `getItemsByType`). **Tests** — `src/lib/validation/item.test.ts` gained two cases (defaulting to `[]`, de-duplicating a submitted list) for both schemas; `npm test` 9 files / 80 tests → 82 tests. The ownership-filtering query and the picker component got no unit tests, consistent with the rest of `src/lib/db` (Prisma + `auth()`, no mocking harness) and React components (out of scope per the Testing standard). No DB migration (`ItemCollection` already existed), no new packages, no new env vars; the build route list is unchanged. Verified with Playwright on the dev server as `demo@devstash.io`: created a snippet with two collections selected (React Patterns, DevOps) → drawer showed both; edited it to swap DevOps for AI Workflows → drawer updated to the new pair; edited again to deselect everything → the Collections section stopped rendering (zero is valid and persists); deleted the test item afterward, seed data unaffected. Lint, `npm test`, and `npm run build` pass. Merged to `main`, branch `feature/item-collections` deleted.
- **2026-09-11** — Collections Pages (Completed): built `/collections` and `/collections/[id]` (inline request, no `context/features/` spec), the two routes the sidebar's "View all collections" link and every `CollectionCard` already pointed at (previously 404s). **`/collections`** (`src/app/(app)/collections/page.tsx`) lists every one of the user's collections via `getRecentCollections()` called with no `limit` — that param changed from a defaulted `limit = 6` to an optional `limit?: number` so omitting it (an undefined `take` is a no-op for Prisma) returns the full set instead of the dashboard's capped 6 or the sidebar's capped 50; renders the same `CollectionCard` grid the dashboard's Recent Collections section uses. **`/collections/[id]`** (`src/app/(app)/collections/[id]/page.tsx`) shows one collection's items: new `getItemsByCollection(collectionId)` in `src/lib/db/items.ts` (new `CollectionSummary` and `ItemsByCollection` interfaces) does a single scoped `Collection.findFirst({ where: { id, userId } })` with a nested `items` include, returning `null` for an unknown or foreign id so the page can `notFound()` — mirrors `getItemsByType`'s shape and reuses the same private `toItemWithType` mapper. Since one collection can mix item types (unlike a `/items/[type]` page), items are split into three buckets — `otherItems` (snippet/prompt/command/note/link) rendered in the `ItemCard` grid, `imageItems` in the `ImageCard` grid, `fileItems` in the `FileRow` list — each in its own labeled section ("Items"/"Images"/"Files") that only renders when non-empty, so a single-type collection shows one section and a mixed one shows several; grid/list classes and the header icon-tile layout were copied from `/items/[type]/page.tsx` for visual consistency. Both pages self-guard with `auth()` → redirect to `/sign-in?callbackUrl=...` (the `[id]` page URL-encodes the id-bearing path, matching `/items/[type]`'s pattern). No new pure logic was added (both new/changed functions are Prisma + `auth()` queries, consistent with the rest of `src/lib/db` having no unit tests), so `npm test` stays at 9 files / 82 tests. No DB migration, no new packages, no new env vars. Lint, `npm test`, and `npm run build` pass; the build route list adds `/collections` and `/collections/[id]`. Browser verification (both pages rendering, the sidebar/dashboard links resolving instead of 404ing, the 404 on an unknown/foreign id) was left to the user. Merged to `main`, branch `feature/collections-pages` deleted.
- **2026-09-11** — Collection Actions: Edit, Delete, Favorite (Completed): built per an inline request (no `context/features/` spec) to let a user edit or delete a collection, and to consolidate those actions plus a Favorite placeholder behind a 3-dot menu on collection cards. **Query** — `src/lib/db/collections.ts` extracted the item-type-tallying logic `getRecentCollections` already had into a shared `toCollectionWithStats()` mapper (plus a `COLLECTION_WITH_ITEMS_INCLUDE` Prisma include constant) so it and the two new functions compute stats identically; `updateCollection(id, data)` does an ownership `findFirst` then `prisma.collection.update` on `name`/`description`, returning the refreshed `CollectionWithStats` (`null` for a foreign/unknown id); `deleteCollection(id)` does the same ownership check then `prisma.collection.delete`, returning `boolean`. Deleting a collection never deletes its items — the schema's `ItemCollection.collection` relation already has `onDelete: Cascade`, so only the join rows for that collection disappear; `Item` rows and their membership in other collections are untouched (this needed no new code, just verification). **Validation** — `src/lib/validation/collection.ts` gains `updateCollectionSchema` (currently identical to `createCollectionSchema` — kept as a separate export so the two can diverge later) and `UpdateCollectionInput`. **Actions** — `src/actions/collections.ts` gains `updateCollection(collectionId, input)` and `deleteCollection(collectionId)`, both following the existing `ActionResult<T>` contract (`requireUserId` guard → Zod validate for update → scoped query → typed data or "not found"/generic error), mirroring `updateItem`/`deleteItem` in `src/actions/items.ts`. **UI** — three new components under `src/components/collections/`: `EditCollectionDialog` (controlled `open`/`onOpenChange`, so it can be triggered from either the detail page's button or a card's dropdown item; re-seeds its form from the `collection` prop when `open` flips to `true` using React's render-time "adjust state when a prop changes" pattern — an earlier `useEffect`-based version tripped the `react-hooks/set-state-in-effect` lint rule), `DeleteCollectionDialog` (controlled, `AlertDialog` confirmation whose copy explicitly says items won't be deleted, mirrors `DeleteItemDialog`), and `CollectionActionsMenu` (the 3-dot `DropdownMenu` with Edit/Favorite/Delete — Favorite has no `onClick` handler at all, matching the precedent set by `ItemActionBar`'s not-yet-wired Pin/Favorite buttons). `CollectionCard.tsx` (shared by `/collections` and the dashboard's Recent Collections grid) became a `"use client"` component: the card body is now a `<button onClick={() => router.push(...)}>` (was a `<Link>`) with the actions menu positioned as an absolutely-positioned sibling in the top-right corner so its clicks never bubble into the navigate handler; it owns `editOpen`/`deleteOpen` state and renders both dialogs, refreshing via `router.refresh()` on success. A new `CollectionDetailActions.tsx` renders the same three buttons (not a dropdown — full-width Favorite/Edit/Delete buttons) in the `/collections/[id]` header, with Delete redirecting to `/collections` on success instead of refreshing in place. **Tests** — no new pure logic was added (the new `db`/`action` functions are Prisma + `auth()`, and the new components are UI, both out of scope for Vitest per this repo's established pattern), so `npm test` stays at 9 files / 82 tests. No DB migration, no new packages, no new env vars; the build route list is unchanged. Verified with Playwright on the dev server as `demo@devstash.io` using disposable test collections (not seed data): edited a collection's name from the `/collections` grid dropdown → name updated live in the card, sidebar, and dialog with a "Collection updated." toast; deleted a collection with 4 mixed-type items (2 items, 1 image, 1 file) from its detail page → redirected to `/collections`, the collection count dropped but the dashboard's Total Items stat and Recent Items list were unchanged, confirming the items survived; deleted a second collection from a dashboard card's dropdown → toast, count updated, no navigation; clicking a card's Favorite menu item and clicking elsewhere on a card were both confirmed not to trigger the wrong action (no accidental navigation from the dropdown, no accidental menu open from the card body). Lint, `npm test`, and `npm run build` pass. Merged to `main`, branch `feature/collection-actions` deleted.
- **2026-09-11** — Global Search / Command Palette (Completed): built per `context/features/global-search-spec.md` — a Cmd+K/Ctrl+K command palette with client-side fuzzy search across items and collections. **Primitive** — new `src/components/ui/command.tsx`: shadcn-style `Command`/`CommandDialog`/`CommandInput`/`CommandList`/`CommandEmpty`/`CommandGroup`/`CommandItem`/`CommandSeparator` wrapping the `cmdk` package (added as a dependency) inside this project's existing base-ui `Dialog` (a sr-only `DialogTitle`/`DialogDescription` satisfy a11y since the palette has no visible header); `CommandDialog` accepts a `commandProps` passthrough so callers can override `Command`-level props like `filter`. **State** — `src/components/search/command-palette-provider.tsx` is a client context (`open`/`setOpen`) that also owns the single global `keydown` listener for the Cmd+K/Ctrl+K toggle; `CommandPaletteProvider` wraps the `(app)` layout alongside the existing `ItemDrawerProvider` (both providers now wrap the whole layout rather than just `<main>`, so `<CommandPalette>` can reach both `useCommandPalette()` and `useItemDrawer()`). **Data** — `src/lib/db/items.ts` gains `getSearchableItems()`, a lightweight `{id, title, itemType, preview}` projection over every item the user owns (`preview` collapses `content ?? description ?? url` to one line, truncated to 100 chars); collections reuse the existing `getRecentCollections()` (now called once with no `limit` in the layout — the sidebar's capped view is derived via `.slice()` instead of a second query) since it already returns `itemCount`. Both are pre-fetched once in `src/app/(app)/layout.tsx` and passed into `<CommandPalette>`. **UI** — `src/components/search/CommandPalette.tsx` renders grouped Items (type icon + title + preview) and Collections (name + item count) sections; selecting an item calls `openItem(id)` (opens the existing item drawer), selecting a collection `router.push`es to `/collections/[id]`; `src/components/layout/TopBar.tsx`'s search `Input` is now read-only and opens the palette on click, with its placeholder showing a "(⌘K)" hint. **Search accuracy fix (same-day follow-up):** the first pass embedded each item/collection's raw database id in the string cmdk scored against, so cmdk's default subsequence-based fuzzy matcher treated a random 25-character cuid as containing almost any short query, producing false-positive results (e.g. searching "snippet" surfaced unrelated items). Fixed by adding a custom, unit-tested scorer — `src/lib/search/fuzzy.ts` (`fuzzyScore`, `commandFilter`) — that scores a contiguous/word-boundary substring match highest and only accepts a looser, order-preserving character match when it's packed within a tight window (query length + small slack), rejecting matches scattered across a long string; `CommandItem`'s `value` was changed to just the title (no id), with `itemType.name`/`preview` passed via cmdk's `keywords` prop (scored at an 0.85 discount) instead, and the custom filter is wired in via `commandProps={{ filter: commandFilter }}`. **Tests** — `src/lib/search/fuzzy.test.ts` (12 cases: substring vs. word-boundary vs. positional scoring, tight-fuzzy-match acceptance, scattered-match rejection incl. a cuid-shaped string, case-insensitivity, `commandFilter` value/keyword precedence); `npm test` 9 files / 82 tests → 10 files / 94 tests. No DB migration, no new env vars; the build route list is unchanged (client-only feature). Verified with Playwright on the dev server as `demo@devstash.io`: Ctrl+K opens/closes the palette from anywhere in the app shell and from a fresh keypress after closing via Escape; the TopBar search input opens it on click; typing filters and groups results live; ArrowDown + Enter opens an item's drawer without navigating away; clicking a collection result navigates to `/collections/[id]`; after the fix, searching "snippet" returns exactly the 4 snippet-type items (matching the sidebar's "Snippet 4" count) instead of unrelated items, "docker" returns only the 3 genuinely Docker-related items, a typo ("snipet") still fuzzy-matches, and a nonsense query shows "No results found." Lint, `npm test`, and `npm run build` pass. Merged to `main`, branch `feature/global-search-command-palette` deleted.
- **2026-09-11** — Pagination (Completed): built per `context/features/pagination-spec.md`. **Constants** — new `src/lib/constants/pagination.ts` holds the four named page-size constants from the spec: `ITEMS_PER_PAGE = 21`, `COLLECTIONS_PER_PAGE = 21`, `DASHBOARD_COLLECTIONS_LIMIT = 6`, `DASHBOARD_RECENT_ITEMS_LIMIT = 10` (the dashboard values match what `RecentCollections.tsx`/`RecentItems.tsx` already hardcoded locally — those local consts were removed in favor of the shared ones). **Pure logic** — new `src/lib/pagination.ts`: `parsePageParam` (safely reads a `?page=` search-param into a 1-based integer, defaulting to 1 for anything missing/non-numeric/non-integer/less-than-1), `getPageRange` (page+perPage → Prisma `skip`/`take`), `getTotalPages` (always ≥ 1), `clampPage`, and `buildPageTokens` (the classic first/last/current±1-with-ellipsis windowing algorithm for the numbered page buttons, collapsing once there are more than 7 pages). **Query layer** — `getItemsByType` and `getItemsByCollection` (`src/lib/db/items.ts`) both take a `page` param and now return `totalCount` alongside `items`; each runs a `skip`/`take` `findMany` in parallel with a `count`, so only one page's rows are ever fetched. `getItemsByCollection` was refactored to query the `ItemCollection` join table directly (`prisma.itemCollection.findMany`/`count`) instead of a single nested `Collection.findFirst` with an un-paginated `items` include, since Prisma's nested-relation `skip`/`take` doesn't come with a matching relation-scoped count. A new `getPaginatedCollections(page)` in `src/lib/db/collections.ts` (paired with the existing `toCollectionWithStats`/`COLLECTION_WITH_ITEMS_INCLUDE` helpers) does the same for `/collections`; `getRecentCollections(limit?)` is untouched and still serves the dashboard/sidebar/command-palette's unpaginated, capped fetches. **UI** — new `src/components/shared/PaginationControls.tsx` (server component, pure `<Link href="?page=N">`s styled with the existing `buttonVariants`) renders numbered pages (via `buildPageTokens`) plus prev/next controls that render as non-interactive, 40%-opacity `<span>`s (`pointer-events-none`, `aria-disabled`) when there's no previous/next page, and renders nothing at all when `totalPages <= 1`. Wired into `/items/[type]`, `/collections/[id]`, and `/collections`, each reading `page` from `searchParams` via `parsePageParam` and passing its own `basePath`; each page's item/collection count in the header now reads `totalCount` (the full count) rather than `items.length`/`collections.length` (which is just the current page's slice), and each page's "empty" message now checks `totalCount === 0` instead of the slice length so an out-of-range page number doesn't falsely claim there's nothing at all. **Scope decision:** the spec's requirements bullet named only `/items/[type]` and `/collections/[id]`, but also defined `COLLECTIONS_PER_PAGE` with no listed page to use it on, and the existing `/collections` page was fetching every collection at once — a direct violation of the spec's "don't fetch all resources at once" rule. Asked the user, who confirmed `/collections` should be paginated too; `getRecentCollections()` (no `limit`) was replaced there with the new `getPaginatedCollections(page)`. **Tests** — new `src/lib/pagination.test.ts` (19 cases covering all five pure functions, including the ellipsis-collapsing behavior of `buildPageTokens` for pages near the start/end/middle of a long run); `npm test` 10 files / 94 tests → 11 files / 113 tests. The paginated query functions and `PaginationControls` got no unit tests, consistent with the rest of `src/lib/db` (Prisma + `auth()`, no mocking harness) and React components (out of scope per the Testing standard). No DB migration, no new packages, no new env vars; the build route list is unchanged (pagination is `?page=` query params, not new routes). Verified with Playwright on the dev server as `demo@devstash.io`: `/items/snippet`, `/collections`, and `/collections/[id]` all render with no console errors and their header counts match the sidebar's per-type counts; since the seeded demo data (18 items across 5 collections) is well under `ITEMS_PER_PAGE`/`COLLECTIONS_PER_PAGE` (21), every listing is naturally a single page, so `PaginationControls` correctly renders nothing on all three — the multi-page walkthrough (numbered buttons, ellipsis collapsing, prev/next enabling/disabling across page boundaries) needed more rows than the seed data provides and was left to the user to verify manually. Lint, `npm test`, and `npm run build` pass. Merged to `main`, branch `feature/pagination` deleted.
- **2026-09-18** — Settings Page (Completed): built per an inline request (no `context/features/` spec) to give account-management actions their own page instead of living on `/profile`. New `src/app/(app)/settings/page.tsx` — a protected route following the same self-guard pattern as `/profile` (`auth()` → redirect to `/sign-in?callbackUrl=/settings` if signed out, then again if `getProfileUser()` returns `null`), `force-dynamic`, rendered inside the shared `(app)` layout chrome (so it's also covered by that layout's defense-in-depth session check). The page renders a single "Account" section reusing the existing `Card`/`CardContent` markup that used to live on `/profile`. **Component move:** `src/components/profile/ProfileAccountActions.tsx` → `src/components/settings/AccountActions.tsx` (renamed export `ProfileAccountActions` → `AccountActions`) via `git mv`, no logic changes — still the same Change Password (toggle-revealed form, `POST /api/auth/change-password`) and Delete Account (`AlertDialog` gated on typing the account email, `POST /api/auth/delete-account`) behavior. `src/app/(app)/profile/page.tsx` had its "Account actions" section (heading, `Card`, and the `ProfileAccountActions` import/usage) removed — it now ends at the "Items by type" usage card. **Sidebar link** — `src/components/layout/Sidebar.tsx`'s user-icon dropdown gains a `Settings` (gear icon) `DropdownMenuItem` linking to `/settings`, placed between the existing "Profile" item and the destructive "Sign out" item. **Scope clarification:** the inline request said to move "delete account and forgot password," but the profile page's Account section only ever contained Change Password + Delete Account — there is no separate "forgot password" action there (password reset is a signed-out flow reached from the sign-in page via `/forgot-password`); documented this reading in the feature's Notes before implementing and proceeded with moving Change Password + Delete Account. No new pure logic was added (this is a page/component relocation plus a nav link), so no new Vitest tests — `npm test` stays at 11 files / 113 tests. No DB migration, no new packages, no new env vars; the build route list adds `/settings`. Verified with Playwright on the dev server as `demo@devstash.io`: opened the sidebar dropdown → "Settings" appears between "Profile" and "Sign out" → clicking it navigates to `/settings`, which renders the Password/Delete account controls under an "Account" heading inside the normal app shell; separately confirmed `/profile` no longer shows an Account section (ends at "Items by type"). Lint, `npm test`, and `npm run build` pass. Merged to `main`, branch `feature/settings-page` deleted.
- **2026-09-19** — Favorites Page (Completed): built per `context/features/favorites-spec.md` — a compact, dev-focused `/favorites` route listing every item and collection the current user has favorited. **Data:** `getFavoriteItems()` (new, `src/lib/db/items.ts`) returns a new `FavoriteItem` (`ItemWithType` + `updatedAt`) for `isFavorite: true` items, sorted `updatedAt` desc — there is no dedicated "favorited at" timestamp, so `updatedAt` is used as the proxy per the spec's explicit instruction (toggling `isFavorite` bumps it like any other field). `getFavoriteCollections()` (new, `src/lib/db/collections.ts`) returns a lighter, dedicated `FavoriteCollection` shape (`id`, `name`, `description`, `itemCount` via `_count`, `updatedAt`) for `isFavorite: true` collections, same sort — deliberately not derived from the heavier `getRecentCollections()`/`toCollectionWithStats()` used by the sidebar/dashboard, since a favorites row needs no per-item-type breakdown. **UI:** `src/app/(app)/favorites/page.tsx` is a self-guarded (`auth()` → `/sign-in?callbackUrl=/favorites`), `force-dynamic` server component rendering two sections — "Items (N)" and "Collections (N)" — each a `divide-y` list of dense, `font-mono text-xs` rows (no cards, minimal padding) inside a single bordered container, plus a centered empty state when both are empty. New `src/components/favorites/FavoriteItemRow.tsx` (client leaf, mirrors `FileRow.tsx`'s pattern) is a full-row button showing the type icon (`ItemTypeIcon`), title, a type-colored outline `Badge`, and the row's `updatedAt` via `formatShortDate`; clicking it calls the existing `useItemDrawer().openItem(id)` — no new drawer logic. New `src/components/favorites/FavoriteCollectionRow.tsx` is a plain server-renderable `<Link href="/collections/[id]">` (no client interactivity needed, unlike the item row) showing a generic `Folder` icon, name, a "Collection" badge, item count, and `updatedAt`. **Nav:** `src/components/layout/TopBar.tsx` gained a `Star`-icon link to `/favorites` between the search input and the New Collection/Item buttons, styled as a plain `<Link>` with `buttonVariants({variant:"ghost", size:"icon-sm"})` rather than `Button render={<Link/>}` — matching the established pattern elsewhere in the repo (`FileRow.tsx`'s download link, `PaginationControls.tsx`) that avoids a known base-ui warning when a `Button` render-prop targets a non-native element. No new pure logic was added (the two new query functions are Prisma + `auth()`, and the new row components are UI), so no new Vitest tests — `npm test` stays at 12 files / 123 tests. No DB migration, no new packages, no new env vars; the build route list adds `/favorites`. Verified with Playwright on the dev server as `demo@devstash.io` (against the 7 favorite items / 2 favorite collections marked via direct SQL earlier in the session): the page renders both sections with correct counts, type badges, and dates in recently-favorited order; clicking an item row opens the `ItemDrawer` with the right item; clicking a collection row navigates to `/collections/[id]`; no console errors. Lint, `npm test`, and `npm run build` pass. Merged to `main`, branch `feature/favorites-page` deleted.
- **2026-09-19** — Editor Preferences Settings (Completed): built per `context/features/editor-settings-spec.md` — a Monaco editor preferences section on `/settings` that auto-saves to the database. **Schema/migration** — `editorPreferences Json?` added to `User` via `prisma migrate dev --name add_user_editor_preferences` (a single additive `ALTER TABLE ... ADD COLUMN`, applied to the Neon `development` branch, never `db push`). **Validation** — new pure, Prisma-free `src/lib/validation/editor-preferences.ts`: `EDITOR_FONT_SIZES` (12/13/14/16/18/20), `EDITOR_TAB_SIZES` (2/4/8), `EDITOR_THEMES` (`vs-dark`/`monokai`/`github-dark`), the `EditorPreferences` interface, `DEFAULT_EDITOR_PREFERENCES` (14px/2/wrap on/minimap off/`vs-dark`), `editorPreferencesSchema` (Zod, refining font/tab size against the allowed sets), and `parseEditorPreferences(raw)` which falls back to the defaults for `null`, a non-object, or any invalid/missing field rather than throwing — this is what makes reading the JSON column safe against a never-saved or future-shape value. **Query/action** — `getEditorPreferences()`/`updateEditorPreferences()` added to `src/lib/db/user.ts` (the latter needed a `data as unknown as Prisma.InputJsonObject` cast since `EditorPreferences` has no index signature); `src/actions/editor-preferences.ts` wraps the update in the project's `requireUserId`/Zod-`safeParse`/`ActionResult<T>` pattern used by every other action file. **Context** — `src/components/editor/editor-preferences-provider.tsx` (`EditorPreferencesProvider`/`useEditorPreferences()`) holds the live preferences object; `setPreferences()` updates state immediately and fires the server action in a `useTransition`, toasting `"Editor preferences saved."` on success or the action's error on failure — there is no separate save button anywhere. The provider wraps the whole `(app)` layout (alongside the existing `ItemDrawerProvider`/`CommandPaletteProvider`), seeded from a new `getEditorPreferences()` call added to the layout's existing `Promise.all`, so both the Settings page and every `CodeEditor` instance read one shared, already-hydrated source of truth. **New UI primitives** — `src/components/ui/select.tsx` and `src/components/ui/switch.tsx`, shadcn-style wrappers over `@base-ui/react`'s `Select`/`Switch` (no select or switch component existed in the repo yet), styled to match the existing `dropdown-menu.tsx`/`input.tsx` tokens. **Settings UI** — `src/components/settings/EditorPreferencesSettings.tsx` renders the three dropdowns and two toggles via `useEditorPreferences()`, with a small "Saving…"/"Changes save automatically." status line; wired into a new "Editor" section on `/settings/page.tsx`, above the existing "Account" section. **Monaco wiring** — `src/components/items/CodeEditor.tsx` now reads `useEditorPreferences()` instead of hardcoded constants: `fontSize`/`tabSize`/`wordWrap`/`minimap.enabled` come straight from the preferences object, `lineHeight` is derived from `fontSize` at the original 16/11 ratio, and `beforeMount` now registers three Monaco themes (`devstash-dark` — the original hand-tuned neutral palette, now the `vs-dark` preference's target; new `devstash-monokai` and `devstash-github-dark` palettes) instead of one, selected via a `MONACO_THEME_NAMES` lookup. This applies identically in the item drawer's read view, its edit mode, and `NewItemDialog` since all three go through the same `CodeEditor`. **Tests** — new `src/lib/validation/editor-preferences.test.ts` (10 cases: schema accept/reject per field, and `parseEditorPreferences` fallback behavior for null/non-object/incomplete/invalid-enum/valid-passthrough); `npm test` 11 files / 113 tests → 12 files / 123 tests. The DB/action functions and the two new UI primitives got no unit tests, consistent with the rest of `src/lib/db`/`src/actions` (Prisma + `auth()`, no mocking harness) and React components (out of scope per the Testing standard). No new packages, no new env vars; the build route list is unchanged (settings additions render inside the existing `/settings` route). Verified with Playwright on the dev server as `demo@devstash.io` (after a required dev-server restart to pick up the regenerated Prisma Client — schema/client changes aren't hot-reloaded): the Settings page's Editor section round-trips a font-size change through a full page reload; the `github-dark` theme's `#0d1117` background was confirmed via computed style on `.monaco-editor`; word wrap, minimap, and the larger font size were all visually confirmed applied in both the drawer's read view and its edit mode for an existing snippet; the Word wrap switch was toggled off and back on to confirm the control round-trips. Test changes (font size 16→12, a word-wrap off/on round trip) were reverted back to the demo user's prior saved values before finishing. Lint, `npm test`, and `npm run build` pass. Merged to `main`, branch `feature/editor-preferences-settings` deleted.
- **2026-09-19** — Favorite Toggle (Drawer, Collection Page, Cards) (Completed): wired up the several Favorite buttons across the app that only changed an icon's fill color with no persistence, per an inline request (no `context/features/` spec). **Queries** — `toggleItemFavorite(id)` (new, `src/lib/db/items.ts`) and `toggleCollectionFavorite(id)` (new, `src/lib/db/collections.ts`) each do an ownership `findFirst`, flip `isFavorite` with `prisma.*.update`, and return the refreshed `ItemDetail`/`CollectionWithStats` (`null` for a foreign/unknown id, nothing written) — `toggleCollectionFavorite` reuses the existing `toCollectionWithStats`/`COLLECTION_WITH_ITEMS_INCLUDE` helpers so its stats match `updateCollection`'s. **Actions** — `toggleItemFavorite`/`toggleCollectionFavorite` added to `src/actions/items.ts`/`src/actions/collections.ts`, both following the established `requireUserId` → scoped query → `ActionResult<T>` contract (no Zod payload beyond the id) used by every other mutation in those files. **Wiring:** `ItemActionBar.tsx`'s Favorite button (the drawer's read view) now takes an `onToggleFavorite` prop that `ItemDrawer.tsx` wires to a `handleToggleFavorite` calling the action, updating `loadedItem` via the existing `toPayload()`/`setItem`, and `router.refresh()`. `CollectionActionsMenu.tsx`'s Favorite menu item (used by `CollectionCard.tsx`'s dropdown) and `CollectionDetailActions.tsx`'s Favorite button (`/collections/[id]` header) gained the same `isFavorite`/`onToggleFavorite` prop pair, each owning local optimistic state that reverts on a failed action and calls `router.refresh()` on success. **Cards:** `ItemCard.tsx` and `CollectionCard.tsx` each briefly gained their own hover-revealed favorite-star toggle button (bottom-left / bottom-right corner, mirroring the existing quick-copy button's `event.stopPropagation()` pattern) — both were **removed** after a same-day follow-up request, since the 3-dot menu (collections) and the drawer (items) already cover the toggle and a card-level star was extra surface area the user didn't want; `ItemCard.tsx` ended the feature functionally unchanged from before it started (it already read `item.isFavorite` straight from its server-fetched prop with no local state), while `CollectionCard.tsx` keeps its static, non-interactive title-row star plus the wired dropdown item. **Sync behavior:** because `ItemCard`/`CollectionCard` hold no local favorite state, toggling in the drawer or a menu and calling `router.refresh()` re-fetches the server component tree and the card's star appears/disappears on its own — verified explicitly per the user's request that the drawer toggle updates the card. **Tests** — no new pure logic was added (the new query/action functions are Prisma + `auth()`, consistent with `updateItem`/`updateCollection` having no unit tests); `npm test` stays at 12 files / 123 tests. No DB migration (`isFavorite` already existed on both models), no new packages, no new env vars; the build route list is unchanged. Verified with Playwright on the dev server as `demo@devstash.io`: toggled and reverted favorite status from an item card during mid-feature testing (before that button was removed), from the item drawer (star fill flips immediately, closing the drawer keeps the state, the underlying card's star appears/disappears correctly with no local-state removal needed), from a collection card's dropdown (collection jumps between the sidebar's Favorites/Recent sections), and from the `/collections/[id]` header (same sidebar effect, button label flips Favorite/Unfavorite); no console errors throughout. Lint, `npm test`, and `npm run build` pass. Merged to `main`, branch `feature/favorite-toggle` deleted.
- **2026-09-19** — Favorites Page Client-Side Sorting (Completed): built per an inline request (no `context/features/` spec) to let users sort the `/favorites` lists instead of always seeing the server's `updatedAt` desc order. New pure, Prisma-free `src/lib/favorites-sort.ts` exports `sortFavoriteItems(items, sort)` (`"date" | "name" | "type"`, sorting a shallow copy and defaulting to the existing `updatedAt` desc order; `"type"` groups by `itemType.name` then breaks ties by title) and `sortFavoriteCollections(collections, sort)` (`"date" | "name"` only — collections have no item-type concept). Two new client components replace the inline item/collection rendering blocks in `src/app/(app)/favorites/page.tsx`: `FavoriteItemsSection` and `FavoriteCollectionsSection`, each holding its own `useState` sort selection (independent per section) and a small shadcn `Select` (reused from the Editor Preferences settings work) in the section header next to the "Items (N)"/"Collections (N)" count, memoizing the sorted list via `useMemo`. The page itself stays a server component — it still fetches via `getFavoriteItems()`/`getFavoriteCollections()` unchanged and just passes the arrays down; sorting happens entirely client-side with no new queries, no new server actions, and no page reload. **Tests** — new `src/lib/favorites-sort.test.ts` (6 cases: date/name/type ordering for items, date/name ordering for collections, and a non-mutation check for both); `npm test` 12 files / 123 tests → 13 files / 130 tests. The two new section components got no unit tests, consistent with the rest of the repo (React components out of scope per the Testing standard). No DB migration, no new packages, no new env vars; the build route list is unchanged. Verified with Playwright on the dev server as `demo@devstash.io`: opened `/favorites` (9 favorite items, 2 favorite collections) → Items sort defaulted to "date"; switching to "Name" alphabetized all 9 rows; switching to "Type" grouped Command/Command → File → Image → Link/Link/Link → Note → Snippet, alphabetized by type then title within each group; the Collections dropdown showed only Date/Name (no Type option) and switching to "Name" re-sorted independently of the Items section's selection. No console errors. Lint, `npm test`, and `npm run build` pass. Merged to `main`, branch `feature/favorites-sorting` deleted.
- **2026-09-19** — Pinned Items (Completed): built per `context/features/pinned-spec.md` to make the drawer's previously inert Pin button functional. **Query** — `toggleItemPin(id)` (new, `src/lib/db/items.ts`) mirrors `toggleItemFavorite`: ownership `findFirst`, flips `isPinned` with `prisma.item.update`, returns the refreshed `ItemDetail` (`null` for a foreign/unknown id). `getItemsByType`, `getItemsByCollection`, and `getRecentItems` all had their `orderBy` changed to a two-key array (`isPinned: "desc"` then `createdAt: "desc"`, with `getItemsByCollection`'s keyed through its `ItemCollection.item` relation) so pinned items sort to the top of the type pages, collection pages, and the dashboard's Recent Items list; `getPinnedItems` (the dashboard's dedicated Pinned section) already only returned pinned rows and needed no change. **Action** — `toggleItemPin` added to `src/actions/items.ts` following the established `requireUserId` → scoped query → `ActionResult<T>` contract used by every other mutation in that file. **Wiring** — `ItemActionBar.tsx`'s previously handler-less Pin button now takes an `onTogglePin` prop; `ItemDrawer.tsx`'s new `handleTogglePin` applies an optimistic `isPinned` flip via `setItem` before the action resolves, reverts it if the action fails (with a `toast.error`), and on success syncs from the returned `ItemDetail`, shows `toast.success` ("Item pinned."/"Item unpinned."), and calls `router.refresh()` so listings re-sort immediately. Per the spec, `ItemCard`'s pin icon stays a static, non-interactive indicator and collections remain unpinnable. No new pure logic was added (the new query/action functions are Prisma + `auth()`, consistent with `toggleItemFavorite`/`toggleCollectionFavorite` having no unit tests); `npm test` stays at 13 files / 130 tests. No DB migration (`isPinned` already existed on `Item`), no new packages, no new env vars; the build route list is unchanged. Verified with Playwright on the dev server as `demo@devstash.io`: opened the "Docker Documentation" item's drawer and clicked Pin — the icon filled immediately (optimistic update), the item jumped to the top of both the dashboard's Pinned section and Recent Items; toggled it back off to restore the item's original state; on `/items/command`, confirmed the two already-pinned seed items ("Clean Install Dependencies", "Undo Last Commit (Keep Changes)") both sort ahead of the three unpinned commands. Lint, `npm test`, and `npm run build` pass. Merged to `main`, branch `feature/pinned-items` deleted.
- **2026-09-20** — Homepage Mockup (Completed): built a static marketing homepage prototype per `context/features/homepage-mockup-spec.md`, output to `prototypes/homepage/` (`index.html`, `styles.css`, `script.js`) — a standalone HTML/CSS/JS page, not wired into the Next.js app, its routing, or auth. **Nav:** fixed top bar (logo, Features/Pricing links, Sign In/Get Started) that gains a blurred/opaque background past an 8px scroll threshold. **Hero:** gradient headline + subheadline + CTAs above a three-part "chaos to order" visual — a chaos box with 8 floating icons animated via `requestAnimationFrame` (drift, wall-bounce, rotate/scale pulse, mouse-repel), a CSS-pulsing arrow, and a "...with DevStash" dashboard mockup (colored sidebar nav items with icon+label, a search-bar + "+ New" top bar, and a 6-card grid with type-colored top borders, an icon, and two skeleton text lines per card so it doesn't read as empty). **Features:** 6 cards (Snippets, Prompts, Search, Commands, Files & Docs, Collections) each tinted with its item-type accent color. **AI section:** Pro-badge + checklist column beside a code-editor mockup with a simulated "AI Generated Tags" demo. **Pricing:** Free vs. Pro cards (Pro badged "Most Popular") with a working monthly/yearly toggle swapping $8/mo ↔ $72/yr via a small state object in `script.js`. **CTA** and **footer** (link columns, JS-set copyright year) close out the page. Scroll-triggered fade-ins use `IntersectionObserver`. Responsive breakpoints stack the chaos/arrow/dashboard trio vertically under 900px (arrow rotated 90° to point down) and collapse the feature/pricing grids to fewer columns down to 640px. **Two follow-up refinement rounds (same day, inline requests):** (1) replaced the placeholder text/emoji chaos icons with real inline-SVG marks — GitHub's official octocat path, Slack's 4-color brand mark, VS Code's ribbon logo, and Notion's page-fold mark (all embedded directly, no external requests) alongside cleaner line icons for browser tabs/terminal/text file/bookmark — and reduced the mouse-repel reaction (radius 90→60, strength 6→2.5, force multiplier 0.08→0.04, max drift speed 2.2→1.4) since it felt too aggressive. (2) The dashboard mockup looked sparse, so it gained the search-bar/"+ New" top bar and per-card icon+skeleton-line content described above, plus text labels on the sidebar nav items — all pure markup/CSS additions, no JS changes. No DB migration, no new packages, no new env vars, no changes to the Next.js app itself (`npm test`/`npm run build` don't apply to a static prototype outside `src/`); verification was left to the user opening `prototypes/homepage/index.html` directly in a browser. Merged to `main`, branch `feature/homepage-mockup` deleted.
- **2026-09-20** — Homepage (Completed): replaced the placeholder `/` route with a real, public marketing homepage per `context/features/homepage-spec.md`, ported from the static `prototypes/homepage/` mockup and outside the `(app)`/`(auth)` route groups (no `auth()` guard, so signed-in users still see it). `src/app/page.tsx` is a static server component (prerendered) that composes `Navbar`, `HeroSection`, `FeaturesSection`, `AiSection`, `PricingSection`, `CtaSection` and `Footer` from the new `src/components/homepage/` folder and sets its own title/description metadata. **Client leaves:** `Navbar` (scroll listener toggles a blurred/opaque background past 8px), `ChaosAnimation` (rAF drift/bounce/pulse/mouse-repel loop scoped to its own box, cleaned up on unmount via `cancelAnimationFrame`/`ResizeObserver`/listener removal, and rendered static when `prefers-reduced-motion` is set), `ScrollFadeIn` (one shared `IntersectionObserver` wrapper reused by every section), and `PricingToggle`. `PricingToggle` only owns the yearly boolean and exposes it as a `data-yearly` attribute on a `group/pricing` wrapper, so the server-rendered pricing cards swap $8/mo ↔ $72/yr with `group-data-[yearly=true]/pricing:` variants and never need to become client components. **Server pieces:** `DashboardMock` (decorative miniature dashboard using `ItemTypeIcon` + `getItemTypeColor`), the features grid (shadcn `Card`, accents from `getItemTypeColor`; Search uses its own indigo since it isn't an item type, Collections uses the link emerald; the hover ring uses a `--feature-color` custom property, deliberately not `--accent`, which would shadow the theme token), the AI section (shadcn `Badge`, a token-array code mock), pricing (`Card`, "Most Popular" badge via `overflow-visible`), CTA, and a footer whose copyright year is `new Date().getFullYear()` computed server-side. `chaos-icons.tsx` holds the inline-SVG Notion/Slack/VS Code marks (reusing `GitHubIcon`) plus lucide icons for the rest — no new dependencies. `src/components/homepage/styles.ts` exports `ctaClasses()` so navigation `<Link>`s reuse the shadcn `buttonVariants` with the blue→indigo gradient (avoids the base-ui `Button render={<Link/>}` warning the repo already sidesteps). The only global CSS change is an `@theme` `--animate-arrow-pulse` keyframe in `globals.css` for the hero arrow (rotated 90° under `lg` so it points down on mobile). **Links:** Sign In → `/sign-in`; Get Started / Get Started Free / Upgrade to Pro → `/register`; Features/Pricing and "See Features" → in-page anchors; Company/Legal footer links stay `#` placeholders. **Deliberately omitted:** the prototype's `scroll-behavior: smooth` — a page-scoped rule on `<html>` risks Next's dev warning on route changes, so anchors jump instantly. No server actions or `src/lib` utilities were added, so no new Vitest tests (`npm test` stays at 13 files / 130 tests); no DB migration, no new packages, no new env vars; `proxy.ts` and existing routes untouched. Verified with Playwright on the dev server: full page renders at 1440px and 390px with no console errors and no horizontal overflow; the pricing switch flips $8/mo ↔ $72/yr in both directions; every link `href` matches the spec's routing table. Lint, `npm test`, and `npm run build` pass. Merged to `main`, branch `feature/homepage` deleted.
- **2026-09-20** — Mobile Top Bar Declutter (Completed): the dashboard top bar overflowed below the `lg` breakpoint — a Playwright inspection at 390px showed 509px of content in a 390px viewport, so **New Item** was clipped entirely off-screen, **New Collection** was cut off mid-label, and the search input was squeezed to a 44px placeholder-less box. Fixed with two responsive changes in `src/components/layout/TopBar.tsx`. (1) **Search** — below `md` the read-only input is hidden and a ghost `Search` icon button (`aria-label="Search"`) in the right-hand group opens the command palette; from `md` up the input is unchanged. (2) **Create actions** — below `sm` the labeled **New Collection** / **New Item** buttons are hidden and replaced by a single `+` `DropdownMenu` (`aria-label="Create new"`, content `align="end"` at `w-44` to override the menu's default anchor-width) with a `Plus` "New Item" item and a `FolderPlus` "New Collection" item; from `sm` up the labeled buttons return. **Favorites** stays its own icon at every width. To let a menu item open a dialog, `NewItemDialog` and `NewCollectionDialog` gained optional `open` / `onOpenChange` / `showTrigger` props (open state is `controlledOpen ?? internalOpen`; a local `setOpen` writes the internal state and calls `onOpenChange`), so with no props they behave exactly as before — the `defaultType` / `triggerLabel` "New {Type}" button on `src/app/(app)/items/[type]/page.tsx` is untouched. `TopBar` now owns `itemDialogOpen` / `collectionDialogOpen` state and mounts each dialog **once** with `showTrigger={false}`, shared by the desktop buttons and the mobile menu (no duplicated instances). No server actions or `src/lib` utilities changed, so no new Vitest tests (`npm test` stays at 13 files / 130 tests). No DB migration, no new packages, no new env vars; the build route list is unchanged. Verified with Playwright on the dev server as `demo@devstash.io`: header overflow is 0px at 320 / 390 / 640 / 768 / 1024px with every control inside the viewport; at 390px the `+` menu lists both items, each opens its dialog, Escape closes it, and the search icon opens the command palette; at 1280px the labeled New Item button and the `/items/snippet` "New Snippet" button still open their dialogs; no console errors. Deferred from the audit (not built): icon-only labeled actions, hiding the "DevStash" wordmark on mobile, and moving Favorites into the sidebar drawer. Lint, `npm test`, and `npm run build` pass. Merged to `main`, branch `feature/mobile-topbar-declutter` deleted.
- **2026-09-21** — UI Review Fixes (Completed): applied the high and medium findings from a `ui-reviewer` audit of the homepage and dashboard area (checked at 1440 / 768 / 390px). (1) **Item drawer on mobile** — the sheet was only ~293px wide at 390px because the base `data-[side=right]:w-3/4` outranks a plain `w-full`, so `ItemDrawer.tsx` now passes `data-[side=right]:w-full`; `ItemActionBar.tsx` also got `flex-wrap`, so Edit and Delete are reachable instead of clipped off-screen. (The drawer's `sm:max-w-md` is likewise overridden by the base `max-w-sm`, so desktop width is unchanged at 384px — left alone.) (2) **Mobile sidebar** — `SidebarMobile.tsx` hides the built-in Close and renders its own "Navigation" header row with a `SheetClose`, so Close no longer covers the "Types" section toggle. (3) **Recent Items** — `getRecentItems` in `src/lib/db/items.ts` reverted to `createdAt desc` only; the pin-first ordering had made the first 6 rows duplicate the Pinned section. Pin-first sorting remains on the type and collection listings, as `pinned-spec.md` asked. (4) **Dates** — favorites item and collection rows now read "Updated Sep 19" (they show `updatedAt`, while cards elsewhere show the created date). (5) **New Item dialog** — the Cancel/Create row is `sticky -bottom-6` inside the dialog's scroll area so it stays visible at 1440x900. (6) **Titles** — `ItemCard` and the drawer header title use `line-clamp-2 break-words` instead of `truncate`. (7) **Favorites sort selects** — a visible "Sort by" label, an `aria-label` per select, and `items={SORT_LABELS}` on the `Select` root so the trigger shows "Date" rather than the raw value `date`. (8) **Headings** — `/settings` and `/profile` gained an `<h1>`. (9) **Homepage mobile nav** — `Navbar.tsx` has a hamburger (below `sm`) that reveals Features, Pricing, and Sign In; the header's Sign In button is hidden below `sm` because Sign In + Get Started + the menu button overflowed the 390px header. **Skipped:** the homepage social-proof finding — there is no real testimonial, logo, or user-count material, and inventing it would be fabricated content; it needs real input from the user. Low-severity findings were out of scope. No server actions or pure utilities changed, so no new Vitest tests (`npm test` stays at 13 files / 130 tests). No DB migration, no new packages, no new env vars; the build route list is unchanged. Verified with Playwright as `demo@devstash.io`: the mobile drawer is 390px wide with Edit/Delete on-screen, the sidebar header and Close no longer overlap, the dialog footer stays pinned, titles wrap to two lines, favorites selects show "Date" with accessible names, both new `<h1>`s render, the mobile menu opens on the homepage, and Recent Items lists only genuinely recent rows; no console errors. Lint, `npm test`, and `npm run build` pass. Merged to `main`, branch `feature/ui-review-fixes` deleted.
- **2026-09-22** — Logo Links (Completed): the DevStash logo (gradient icon tile + wordmark) now links to the homepage in the two places it was static text. (1) **Dashboard header** — `src/components/layout/TopBar.tsx` wraps the logo in a `next/link` `<Link href="/" aria-label="DevStash home">`; the sidebar-toggle button stays outside the link. Every `(app)` page renders inside this shell and the `(app)` layout already guarantees a signed-in user, so this covers the logged-in dashboard. `/` has no `auth()` guard, so signed-in users can reach the marketing homepage. (2) **Auth pages** — `src/app/(auth)/layout.tsx` wraps its shared logo in the same `<Link href="/">` (with `self-center` so the link hugs the logo instead of stretching across the form column), which covers sign-in, register, **and** forgot-password / reset-password since they share that layout — the latter two were explicitly requested. Both links get `rounded-md outline-none focus-visible:ring-3 focus-visible:ring-ring/50` for a visible keyboard focus ring; size, gradient, and wordmark are unchanged. Deliberately did not reuse `src/components/homepage/HomeLogo.tsx` — its sizing is tuned for the homepage navbar, and wrapping the existing markup was the smaller change. No server actions or `src/lib` utilities changed, so no new Vitest tests (`npm test` stays at 13 files / 130 tests). No DB migration, no new packages, no new env vars; the build route list is unchanged. Verified: SSR HTML for `/sign-in`, `/register`, `/forgot-password`, and `/reset-password` each contain the logo link with `href="/"`, and with Playwright as `demo@devstash.io` clicking the dashboard header logo navigated to `/` with no console errors; the signed-out visual check of the auth pages was left to the user. Lint, `npm test`, and `npm run build` pass. Merged to `main`, branch `feature/logo-links` deleted. The push for this feature also carried the two previously unpushed UI Review Fixes commits (`8896d9c`, `7d51083`).
- **2026-09-22** — Stripe Core Infrastructure (Phase 1) (Completed): laid the groundwork for Stripe billing per `context/features/stripe-phase-1-spec.md` (plan reference: `docs/stripe-integration-plan.md`) with no user-facing behavior change and nothing enforced yet. Added `stripe@^22.6.2`. `src/lib/stripe.ts` exports `isStripeConfigured()` (secret key + both price IDs set) and a lazy `getStripe()` cached on `globalThis` like `src/lib/prisma.ts` / `src/lib/r2.ts`, which throws a clear error when `STRIPE_SECRET_KEY` is unset and passes no `apiVersion` so upgrading the package is the only thing that moves the API version. New pure, Prisma/`auth`/Stripe-free `src/lib/billing/plans.ts`: `FREE_ITEM_LIMIT = 50`, `FREE_COLLECTION_LIMIT = 3`, `BILLING_PLANS` / `BillingPlan` / `isBillingPlan` / `priceIdForPlan`, `isProSubscriptionStatus` (active/trialing/past_due) and `isTerminalSubscriptionStatus` (canceled/incomplete_expired), `hasProAccess(isPro)` (returns true for everyone when `PRO_GATING_ENABLED === "false"`), the `PRO_TYPE_ERROR` / `ITEM_LIMIT_ERROR` / `COLLECTION_LIMIT_ERROR` messages, and `canCreateItem` (file/image type check runs before the item-count check) / `canCreateCollection` returning a `GateResult`. **Session carries `isPro`:** `src/types/next-auth.d.ts` adds `isPro: boolean` to `Session["user"]` and `isPro?: boolean` to the `next-auth/jwt` augmentation; the edge-safe `auth.config.ts` `session` callback sets `session.user.isPro = token.isPro === true`; `src/auth.ts` gains a `jwt` callback that re-reads `isPro` from the database on every call (a missing/deleted user is treated as free), so a webhook-driven plan change appears on the next `auth()` without re-signing in. It is registered as `callbacks: { ...authConfig.callbacks, jwt }` — a bare `{ jwt }` would replace the whole object and drop the `session` callback, breaking `session.user.id` and every `requireUserId()`. `src/lib/db/current-user.ts` gains a `React.cache`-wrapped `getCurrentUserIsPro()`. `.env.example` documents `PRO_GATING_ENABLED` (defaults ON when unset; `"false"` grants everyone Pro), the `STRIPE_SECRET_KEY` / `STRIPE_WEBHOOK_SECRET` / `STRIPE_PRICE_ID_MONTHLY` / `STRIPE_PRICE_ID_YEARLY` block, notes that `APP_URL` is also used for Stripe return URLs, and ends with a trailing newline; `STRIPE_PUBLISHABLE_KEY` was dropped since Checkout is Stripe-hosted. `proxy.ts` is untouched. **Tests:** new `src/lib/billing/plans.test.ts` covers `canCreateItem`, `canCreateCollection`, both status predicates, `isBillingPlan`, `priceIdForPlan`, and `hasProAccess` with no mocks (`vi.stubEnv` + `vi.unstubAllEnvs()` in `afterEach`) — `npm test` 13 files / 130 tests → 14 files / 165 tests. The `jwt` callback, `getCurrentUserIsPro()`, and `stripe.ts` got no unit tests, consistent with the rest of the repo (Prisma + `auth()`, no mocking harness). No DB migration (`isPro`, `stripeCustomerId`, `stripeSubscriptionId` already exist from `init`), no new routes. Lint, `npm test`, and `npm run build` pass with the build route list unchanged. **Not verified in this session:** the manual browser check of credentials + GitHub sign-in, `/api/auth/session` returning `user.id` and `user.isPro`, and `isPro` flipping without a re-sign-in was not run before completing. Also committed the Stripe integration plan, research prompt, and Phase 2 spec as a separate `docs:` commit. Merged to `main`, branch `feature/stripe-core-infrastructure` deleted.
- **2026-09-22** — Stripe Webhooks, Gating & UI (Phase 2) (Completed): users can now pay for Pro and Free users are limited, per `context/features/stripe-phase-2-spec.md` (plan reference: `docs/stripe-integration-plan.md`). Committed in three parts so gating could be validated separately from billing. **Part A — webhook and sync:** `src/lib/billing/subscription-sync.ts` exports `stripeId(ref)` and `applySubscription(subscription, knownUserId?)`, the single write path for plan state. It resolves the user from `knownUserId` (Checkout's `client_reference_id`), then `subscription.metadata.userId`, then `stripeCustomerId`; an unknown customer logs a warning and returns instead of throwing (a retry could never succeed). It writes the current state rather than a delta (`isPro = isProSubscriptionStatus(status)`, `stripeCustomerId`, and `stripeSubscriptionId` nulled on terminal statuses), which makes every branch idempotent so no processed-events table is needed; a non-Pro event for a subscription id that differs from the user's current one is ignored so a dead old subscription can't revoke a newer one, and `stripeCustomerId` is kept after a cancel. `POST /api/webhooks/stripe` reads the raw body with `request.text()` (signature verification is the only auth — no session, no rate limit): 503 when Stripe/webhook env is missing, 400 for a missing or invalid signature, 500 for our own failures so Stripe retries, 200 for everything handled or ignored. It handles `checkout.session.completed` (subscription mode only, retrieves the subscription first) and `customer.subscription.created|updated|deleted`. `proxy.ts` was not widened. **Part B — checkout, portal, billing UI:** `src/lib/billing/checkout.ts` `createCheckoutUrl` creates and saves the Stripe customer *before* Checkout starts (with `idempotencyKey: devstash-customer-<userId>`, so rapid double clicks yield one customer) and starts a `mode: "subscription"` session with `client_reference_id`, `subscription_data.metadata.userId`, `allow_promotion_codes`, and success/cancel URLs on `/settings?checkout=…`; `createPortalUrl` returns `null` when there is no customer. The base URL comes from `APP_URL` (trailing slashes stripped), falling back to the `origin` header, since Server Actions have no `Request` for `getAppBaseUrl`. New `src/actions/billing.ts` (`"use server"`, `ActionResult<T>`) exposes `createCheckoutSession(plan: unknown)` and `createPortalSession()`, each returning `{ url }` for the client to `window.location.assign` so failures can toast; both run `requireUserId` → validate → `isStripeConfigured()` → rate limit → try/catch, and checkout refuses users already Pro using the real `isPro` (not `hasProAccess()`) so a developer with gating off can still test upgrading. `src/lib/rate-limit.ts` gains a fail-open `billing` limiter (10/hour, keyed on `session.user.id`). `ProfileUser` gains `isPro` and `hasStripeCustomer` booleans (the Stripe ids are never returned). New client leaf `src/components/settings/BillingSettings.tsx`: Free view (badge, `n / 50` and `n / 3` usage meters as native `<progress>` elements to avoid inline styles, Monthly $8 / Yearly $72 selector with yearly preselected and a "Save 25%" note, **Upgrade to Pro**), Pro view (badge + **Manage subscription**), and cancelled view (Free view + a "Billing history" portal link). On `?checkout=success` while not yet Pro it shows "Payment received, activating Pro…" and calls `router.refresh()` every 2 s for up to 5 attempts (each refresh re-runs `auth()`, whose Phase 1 `jwt` callback re-reads `isPro`), then a "taking longer than expected" fallback; `toast.success("Welcome to DevStash Pro!")` and the `?checkout=canceled` toast are ref-guarded so React strict-mode double effects don't duplicate them. The settings page accepts `searchParams`, fetches item/collection stats in parallel with the user, and adds a **Billing** section above **Account** showing the real `user.isPro`. **Part C — gating:** `src/lib/billing/limits.ts` `getItemCreationBlock(itemType)` / `getCollectionCreationBlock()` return an error string or `null` and skip the count query for Pro users or with gating off; they are called in `createItem` and `createCollection` after `requireUserId` and Zod parsing. `POST /api/upload` returns `403 { error: PRO_TYPE_ERROR, code: "UPGRADE_REQUIRED" }` for non-Pro users after the 401 check and before `isR2Configured()` / `formData()`, so a 10 MB body is never buffered or written to R2 just to be rejected. `GET /api/items/[id]/download`, `updateItem`, `deleteItem`, and the favorite/pin toggles are deliberately not gated: a downgraded user keeps all existing data and can still download their files; only creating past the limits is blocked. New `src/components/billing/plan-provider.tsx` (`PlanProvider` / `usePlan()` with `{ hasPro, itemCount, collectionCount }`) is mounted in `src/app/(app)/layout.tsx` and derives its counts from data the layout already fetched (the `count` field of `getItemTypesWithCounts()` and the full collections list), so there are no new queries. New `src/components/billing/UpgradeNotice.tsx` is a `FormNotice`-styled block with a `buttonVariants`-styled `<Link>` to `/settings` (not `Button render={<Link/>}`, per the repo's base-ui workaround). `NewItemDialog` shows a **PRO** badge on File/Image for non-Pro users, renders `UpgradeNotice` in place of `FileUpload` for those types, and disables Create there and at `itemCount >= FREE_ITEM_LIMIT`; `NewCollectionDialog` does the same at `collectionCount >= FREE_COLLECTION_LIMIT` — UX layered on the server enforcement, not a replacement for it. `prisma/seed.ts` now creates the demo user with `isPro: true` and backfills an existing demo user via an idempotent `else if (!user.isPro)` update (it seeds 5 collections against a Free limit of 3, so it would otherwise be permanently over-limit once gating is on). **Part D — account deletion safety:** `POST /api/auth/delete-account` cancels the user's Stripe subscription before `prisma.user.delete` when `stripeSubscriptionId` is set; `resource_missing` (`Stripe.errors.StripeInvalidRequestError`) counts as already gone, and any other Stripe error is logged and returns 500 **without deleting** — an undeleted account is recoverable, a still-billing orphan is not. The Stripe customer is left in place for invoice history. The delete-account dialog copy in `AccountActions.tsx` now says an active subscription is cancelled immediately. `docs/item-crud-architecture.md` no longer describes the Pro gate as deferred. No DB migration and no new env vars beyond Phase 1's; no new packages. **Tests:** no new unit tests, per the spec — the webhook, `subscription-sync.ts`, `checkout.ts`, `limits.ts`, and the actions are Prisma + `auth()` + Stripe glue with no mocking harness in the repo, and the decision logic they call is already covered by Phase 1's `plans.test.ts`; `npm test` stays at 14 files / 165 tests. Lint, `npm test`, and `npm run build` pass; the build route list gains `/api/webhooks/stripe`. **Verification done:** against the running dev server, `POST /api/webhooks/stripe` returned 503 (`STRIPE_WEBHOOK_SECRET` was still empty), `GET` returned 405, and unauthenticated `POST /api/upload` returned 401; the seed was re-run against the Neon `development` branch (endpoint confirmed via the Neon MCP) and `demo@devstash.io` is now `isPro: true` with its 5 collections intact. **Not verified — the spec's manual checks were not run before completing:** signature failure and replay handling, the unknown-customer and out-of-order `deleted` paths, the `past_due` grace period, a real test-card Checkout (monthly and yearly), cancel-in-Checkout, the Billing Portal round trip, re-subscribe reusing the customer, the 50-item / 3-collection / File-Image gating in the browser, the downgraded-user behavior, Pro-account deletion cancelling the subscription, and the billing rate limit. These need the Stripe CLI running with `STRIPE_WEBHOOK_SECRET` set, which was not in place. Local `.env` runs with `PRO_GATING_ENABLED="true"` (the code default is also ON when unset, so this changes behavior for every existing non-Pro account); the owner's own account already has a Stripe customer from an earlier Checkout attempt but no subscription. Merged to `main`, branch `feature/stripe-webhooks-gating-ui` deleted.
- **2026-09-23** — Sidebar Pro Badge (Completed): the sidebar user footer now shows the user's plan (inline request, no `context/features/` spec). `src/components/layout/Sidebar.tsx`: `SidebarUser` gains an optional `isPro` (the session user passed from `src/app/(app)/layout.tsx` already carries it, so no layout change); a new local `ProBadge` renders a blue→indigo gradient (`bg-linear-to-r from-blue-500 to-indigo-500`, matching the homepage CTA) `Sparkles` + PRO pill next to the name in the expanded sidebar and mobile drawer (name truncates, badge stays); the collapsed desktop rail instead shows an `aria-hidden` gradient dot on the avatar corner and the trigger's `aria-label` becomes `Account menu (Pro plan)`. Free users (expanded only) get a thin `Free plan · Upgrade` row above the footer, linking to `/settings#billing` — placed outside the dropdown trigger because a link can't nest in its `<button>`. The Billing `<section>` on `src/app/(app)/settings/page.tsx` gained `id="billing"` + `scroll-mt-6`. Driven by the real `session.user.isPro`, deliberately not `usePlan().hasPro` (true for everyone when `PRO_GATING_ENABLED="false"`). UI only, so no new Vitest tests (`npm test` stays at 14 files / 165 tests). No DB migration, no new packages, no new env vars; build route list unchanged. Lint, `npm test`, and `npm run build` pass. Browser verification (Pro pill, collapsed dot, Free upgrade row) was left to the user. Merged to `main`, branch `feature/sidebar-pro-badge` deleted.
- **2026-09-23** — Gate /items/file and /items/image for Free Users (Completed): Free users can no longer view the File or Image type-listing pages at all — previously only item *creation* was Pro-gated (per Stripe Phase 2), leaving the pages themselves (and any existing items on them) visible to everyone. New `resolveProItemTypeSlug(slug)` in `src/lib/constants/item-types.ts` resolves a `/items/[type]` route slug to `"file"` / `"image"` (accepting the plural form, mirroring the existing singular/plural convention in `getItemsByType`) or `null` for any other slug. `src/app/(app)/items/[type]/page.tsx` calls it right after the existing `auth()` guard, before either data fetch (`getItemsByType` / `getCollectionOptions`): when the slug resolves to a Pro type and `hasProAccess(session.user.isPro)` is false, the page returns early with the "Back to dashboard" link plus a new `ProFeatureUpgrade` component instead of running any query. `ProFeatureUpgrade` (`src/components/billing/ProFeatureUpgrade.tsx`, server component) shows the type's icon/color (`getItemTypeColor`), a "{Type} uploads are a Pro feature" heading, a short description, and an "Upgrade to Pro" link to `/settings#billing` (styled with `buttonVariants`, matching the existing `UpgradeNotice` pattern — a plain `<Link>`, not `Button render={<Link/>}`). Pro users, and everyone when `PRO_GATING_ENABLED="false"`, see the page exactly as before (unaffected — `hasProAccess` short-circuits true). Downgraded users lose the ability to *view* the file/image list pages, unlike the rest of the Pro gating in this app which otherwise lets existing data stay reachable (e.g. downloads, favorite/pin toggles) — a deliberate exception per this feature's explicit request. **Tests:** `src/lib/constants/item-types.test.ts` gained a `resolveProItemTypeSlug` describe block (singular/plural/case-insensitive resolution, `null` for non-Pro and unknown slugs); `npm test` 14 files / 165 tests → 167 tests. The page-level branch and `ProFeatureUpgrade` got no unit tests, consistent with the rest of `src/app/(app)/**` (React components/pages out of scope per the Testing standard). No DB migration, no new packages, no new env vars; the build route list is unchanged. Verified with Playwright on the dev server: signed in as the Pro `demo@devstash.io` user, `/items/file` still rendered the normal file list unaffected; registered a disposable Free test user (`free-gate-test@example.com`, deleted afterward via its own account-deletion API), signed in as it, and confirmed both `/items/file` and the plural `/items/images` rendered the upgrade page (correct heading/description per type, sidebar still shows the PRO badge + item count, "Upgrade to Pro" links to `/settings#billing`) instead of the item list. Lint, `npm test`, and `npm run build` pass. Merged to `main`, branch `feature/gate-file-image-pages` deleted.
